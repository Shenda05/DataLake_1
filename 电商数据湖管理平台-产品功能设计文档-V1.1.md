**电商数据湖管理平台**

**产品功能设计文档**

Function Design Specification

项目名称：电商数据湖管理平台（Web端）

摘要：本文档基于当前项目仓库已实现的功能，对系统的建设目标、业务场景、功能模块、页面设计、核心数据对象和技术实现进行整理说明，作为课程项目交付、系统演示、测试验收和教师审阅的重要依据。

版权所有：除特别允许外，仅供项目组内部教学与课程开发使用。

**修改记录：**

|     |     |     |     |
| --- | --- | --- | --- |
| **日期** | **版本** | **说明** | **作者** |
| 2026/03/27 | V1.0 | 编写“电商数据湖管理平台”产品功能设计文档初版 | 肖溢丹 |
| 2026/04/04 | V1.1 | 基于当前项目已实现功能完善产品功能设计文档，形成课程交付版 | 肖溢丹 |

# 目录

| 章节 | 内容 |
| --- | --- |
| 第1章 | 项目概述 |
| 第2章 | 业务场景说明 |
| 第3章 | 用户角色设计 |
| 第4章 | 产品总体设计 |
| 第5章 | 典型业务流程设计 |
| 第6章 | 功能模块详细设计 |
| 第7章 | 页面清单设计 |
| 第8章 | 核心数据对象设计 |
| 第9章 | 非功能需求设计 |
| 第10章 | 技术落地建议 |
| 第11章 | 项目实施优先级设计 |
| 第12章 | 团队分工设计 |
| 第13章 | 开发阶段规划 |
| 第14章 | 风险与应对方案 |
| 第15章 | 项目亮点总结 |
| 第16章 | 可直接用于课堂汇报的项目说明 |
| 第17章 | 答辩时推荐的一句话定位 |

# 1. 项目概述

## 1.1 项目背景

电商业务每天会持续产生用户、商品、订单、支付、库存、评价和行为日志等多类数据。这些数据来源不同、格式不统一，既包含数据库表，也包含 CSV、JSON、Excel 等文件，容易出现接入分散、口径不一致、查询效率低和人工处理成本高等问题。

为解决课程项目中的数据统一管理、清洗分析和可视化展示需求，本项目设计并实现了一个面向电商场景的数据湖管理平台。平台通过 Web 端把数据接入、数据集管理、查询分析、数据治理、任务调度和日志监控串联起来，形成可演示、可操作、可验证的完整业务闭环。

## 1.2 项目定位

本项目定位为一个面向课程实训场景的电商数据湖管理平台原型系统。系统强调以下特点：

1. 以订单、商品、库存为主线，兼容用户、支付、评价和行为日志等业务域。
2. 以当前项目真实可运行代码为基础，不依赖纯静态页面或 Mock 演示。
3. 以“接入、管理、分析、治理、调度、监控”为主链路，突出平台化和工程化能力。

## 1.3 项目目标

本项目当前阶段的建设目标如下：

1. 支持电商多源数据统一接入，包括文件导入和 `MYSQL` 数据库表导入。
2. 支持数据集、元数据和预览数据的统一管理。
3. 支持条件查询、单表 SQL 查询和电商指标图表分析。
4. 支持基础数据集成能力，并可将集成结果保存为新数据集。
5. 支持算子化数据治理、流程保存、顺序执行和结果输出。
6. 支持 `IMPORT`、`GOVERNANCE` 两类调度任务的配置、执行和监控。
7. 支持基于角色的菜单权限和操作权限控制，满足课程项目的管理与演示需求。

## 1.4 项目主题说明

本项目采用“电商数据湖管理平台”作为统一主题，围绕以下业务域展开：

| 业务域 | 说明 |
| --- | --- |
| `USER` | 用户基础信息数据，用于用户查询和用户-订单关联分析 |
| `PRODUCT` | 商品基础信息数据，用于商品查询、热销排行和分类统计 |
| `TRADE` | 订单交易数据，用于订单量趋势、销售额分析和订单治理 |
| `PAYMENT` | 支付数据，用于支付状态接入和状态标准化 |
| `INVENTORY` | 库存数据，用于库存预警和商品-库存集成 |
| `REVIEW` | 商品评价数据，用于评价数据接入和基础查询 |
| `BEHAVIOR_LOG` | 用户行为日志数据，用于行为日志接入和基础分析 |

## 1.5 项目范围

### 本期建设范围

本期课程交付范围已经覆盖以下模块：

1. 用户登录与权限管理
2. 电商仪表盘
3. 数据源管理
4. 文件导入与数据库表导入
5. 数据集管理与元数据展示
6. 数据预览与分页筛选
7. 条件查询与单表 SQL 查询
8. 电商指标图表分析
9. 数据集成与结果保存
10. 数据治理与治理流程执行
11. 任务调度管理
12. 任务日志监控与失败回放
13. 数据导出
14. 用户与角色权限管理

### 暂不纳入本期范围

以下能力不属于当前课程项目已实现范围：

1. 分布式大数据计算框架接入
2. 实时流式采集与实时计算
3. 跨数据集多表自由 SQL 查询
4. 可视化字段映射式治理编排器
5. 推荐算法、机器学习训练和复杂风控模型
6. 多租户复杂组织权限体系

# 2. 业务场景说明

## 2.1 核心业务场景

### 场景1：多源电商数据统一接入

平台支持从文件和 `MYSQL` 数据库表接入电商业务数据。文件导入支持 `CSV / JSON / Excel`，数据库导入支持自动识别 `Schema / Database`、表列表和样例预览，便于把外部数据快速转化为平台内的数据集。

### 场景2：基础电商数据治理

平台内置通用清洗算子和电商业务算子，可针对订单、商品、支付等数据执行空值填充、去重、金额标准化、时间标准化、分类标准化和状态标准化等处理，并将治理结果输出为新数据集。

### 场景3：基础数据集成分析

平台支持在查询分析页中对两个数据集执行 `JOIN / UNION` 集成，适用于订单-商品、商品-库存、用户-订单等典型场景。集成结果支持预览并保存为新数据集，继续参与后续分析或治理。

### 场景4：查询分析与电商指标展示

平台支持条件查询、单表 SQL 查询和图表分析，能够在当前筛选条件基础上查看订单量趋势、销售额趋势、商品销量排行、分类销售占比和库存预警等电商指标。

### 场景5：自动化任务与失败回放

平台支持将导入记录和治理流程配置为定时任务，支持 `Cron` 调度、立即执行、暂停、恢复和失败日志回放，适合演示自动化数据处理与监控闭环。

## 2.2 电商数据类型说明

| 业务域枚举 | 业务说明 | 当前系统中的典型用途 |
| --- | --- | --- |
| `USER` | 用户基础信息 | 用户查询、用户与订单集成 |
| `PRODUCT` | 商品基础信息 | 商品列表、热销商品排行、分类分析 |
| `TRADE` | 订单交易信息 | 订单量趋势、销售额分析、订单治理 |
| `PAYMENT` | 支付信息 | 支付状态接入、状态标准化 |
| `INVENTORY` | 库存信息 | 低库存统计、库存预警分析 |
| `REVIEW` | 商品评价信息 | 评价数据接入和基础查询 |
| `BEHAVIOR_LOG` | 用户行为日志 | 行为日志接入和筛选查询 |

## 2.3 当前业务闭环

系统当前真实支持的业务闭环如下：

1. 创建并维护数据源。
2. 通过文件或数据库表导入生成数据集。
3. 查看数据集列表、元数据和分页预览。
4. 在查询分析页执行条件查询、SQL 查询、图表分析或数据集成。
5. 在治理页配置治理流程并生成新的输出数据集。
6. 将导入记录或治理流程配置为调度任务。
7. 在日志页查看执行结果、失败原因，并对失败任务执行回放。

# 3. 用户角色设计

## 3.1 角色分类

当前系统按照真实实现提供两类角色：

| 角色 | 角色说明 | 当前定位 |
| --- | --- | --- |
| 管理员（`ADMIN`） | 平台管理角色 | 负责数据源、数据库导入、用户与角色、任务和核心管理操作 |
| 运营分析人员（`OPERATOR`） | 普通业务角色 | 负责查看授权菜单、查询分析、治理执行、日志查看与导出等操作 |

## 3.2 权限边界

### 管理员权限

管理员默认拥有全部菜单权限和全部操作权限，重点包括：

1. 数据源新增、编辑、删除、测试连接、启停
2. `MYSQL` 数据库表导入
3. 数据集删除
4. 治理流程保存与执行
5. 任务配置与任务触发
6. 数据集、查询结果和日志导出
7. 用户管理与角色配置
8. 失败日志回放

### 运营分析人员权限

普通用户默认具备除“用户与权限”外的业务菜单访问能力，并拥有以下典型操作权限：

1. 登录平台并访问已授权页面
2. 查询分析与查询结果导出
3. 治理流程保存与执行
4. 任务配置与立即执行
5. 数据集导出
6. 日志导出与失败回放

### 管理类限制

以下能力默认仅管理员可执行：

1. `source.manage`
2. `import.database`
3. `dataset.delete`
4. `user.manage`
5. `role.manage`

## 3.3 权限同步机制

系统采用 `JWT` 登录鉴权，前端会定时同步当前账号的 profile 信息。角色、菜单或操作权限调整后，现有会话会在同步周期内自动感知变化，并实时更新可见导航和可执行按钮，无需依赖重新登录才能生效。

# 4. 产品总体设计

## 4.1 总体业务闭环

平台总体链路如下：

电商数据接入 → 数据集管理 → 查询分析 / 数据集成 → 数据治理 → 任务调度 → 日志监控

其中，用户与权限模块负责控制访问范围，电商仪表盘负责聚合展示核心状态和快捷入口。

## 4.2 菜单结构设计

当前系统一级菜单与实际路由保持一致：

| 序号 | 菜单名称 | 路由 | 主要内容 |
| --- | --- | --- | --- |
| 1 | 首页 | `/dashboard` | 电商仪表盘、核心指标、快捷跳转 |
| 2 | 数据源管理 | `/data-sources` | 数据源新增、编辑、测试、启停 |
| 3 | 电商数据接入 | `/imports` | 文件导入、数据库表导入、导入历史 |
| 4 | 电商数据集管理 | `/datasets` | 数据集列表、元数据、预览、导出 |
| 5 | 电商查询分析 | `/queries` | 条件查询、SQL 查询、电商指标、数据集成 |
| 6 | 电商数据治理 | `/governance` | 算子管理、治理流程配置与执行 |
| 7 | 电商任务调度 | `/tasks` | 任务创建、编辑、暂停、恢复、执行 |
| 8 | 任务与日志监控 | `/logs` | 日志筛选、详情查看、导出、回放 |
| 9 | 用户与权限 | `/users` | 用户管理、角色权限配置 |

## 4.3 模块关系说明

1. 电商仪表盘展示数据集、数据源、任务和日志的聚合结果，并提供模块级跳转。
2. 数据源管理为数据接入提供来源配置。
3. 电商数据接入生成数据集和元数据。
4. 数据集管理为查询分析、数据治理和导出提供基础数据对象。
5. 查询分析页同时承载基础查询、电商指标和数据集成子标签。
6. 数据治理输出的新数据集可再次进入数据集管理、查询分析和导出链路。
7. 任务调度围绕导入记录和治理流程工作，日志监控负责记录并回放执行结果。

## 4.4 当前系统边界

为保证文档与代码一致，当前系统边界明确如下：

1. 数据集成能力属于“电商查询分析”页内子标签，不是独立一级菜单。
2. 系统没有独立“系统设置”模块。
3. 数据库导入当前仅支持 `MYSQL` 数据源。
4. 数据库导入当前仅支持按 `Schema / Database + 表` 导入，不支持自定义 SQL 导入。
5. SQL 查询限定为当前数据集单表查询。
6. 治理参数当前采用 `JSON` 文本配置，不是可视化字段映射向导。
7. 调度能力采用应用内轮询和 `CronExpression`，不是 Quartz 集群调度。

# 5. 典型业务流程设计

## 5.1 文件导入流程

1. 管理员或具备接入权限的用户在数据源管理页创建 `FILE` 数据源。
2. 进入“电商数据接入”页，选择所属数据源并上传文件。
3. 填写数据集名称、业务域、编码方式、是否首行为表头等参数。
4. 系统识别文件格式并执行解析校验。
5. 导入成功后自动生成数据集、元数据和导入历史记录。
6. 用户可在导入历史中查看导入编号、记录数、状态和错误信息。

## 5.2 数据库表导入流程

1. 管理员先创建 `MYSQL` 类型数据源并保存连接信息。
2. 在“电商数据接入”页选择数据库导入模式。
3. 选择 `MYSQL` 数据源后，系统自动加载可用的 `Schema / Database` 列表。
4. 用户切换 `Schema / Database` 后，系统同步刷新表列表。
5. 选择表后可查看表结构和样例预览，再填写目标数据集名称和业务域。
6. 导入成功后生成数据集和导入历史，失败时保留错误原因。

## 5.3 查询分析流程

1. 用户进入“电商查询分析”页选择目标数据集。
2. 可在条件查询中配置多条件、`AND / OR` 逻辑、排序和分页。
3. 也可切换到 SQL 查询，执行当前数据集的单表 `SELECT` 查询。
4. 查询结果支持统计摘要、图表分析和导出。
5. 用户可切换到电商指标页查看趋势和排行，也可切换到数据集成子标签执行 `JOIN / UNION`。

## 5.4 数据治理流程

1. 用户在治理页选择输入数据集。
2. 从算子列表中添加一个或多个治理步骤。
3. 通过 `JSON` 文本为每个步骤填写参数，并按需要调整执行顺序。
4. 可先保存治理流程，再手动执行。
5. 执行完成后返回治理前记录数、治理后记录数、异常处理条数和输出数据集信息。
6. 若执行失败，页面会展示失败步骤和失败原因。

## 5.5 任务调度与日志流程

1. 用户在任务调度页选择任务类型为 `IMPORT` 或 `GOVERNANCE`。
2. 根据任务类型选择导入记录或治理流程作为执行对象。
3. 填写任务名称、`Cron` 表达式、重试策略和描述后保存任务。
4. 任务可启用、暂停、恢复，也可手动立即执行。
5. 系统按轮询方式调度到期任务，并生成结构化日志。
6. 用户在日志页按类型、状态、操作人和时间范围筛选日志，查看详情或执行失败回放。

# 6. 功能模块详细设计

## 6.1 用户登录与权限管理模块

### 6.1.1 功能目标

负责完成平台登录鉴权、用户维护、角色权限配置和前端权限同步，是整个平台访问控制的基础模块。

### 6.1.2 主要功能

1. 用户名密码登录和空值校验。
2. 登录成功后按当前角色返回菜单权限和操作权限。
3. 用户新增、编辑、停用、删除。
4. 角色说明、菜单权限和操作权限配置。
5. 当前会话自动同步权限变更。

### 6.1.3 页面设计

**登录页**

1. 提供用户名、密码输入框和登录按钮。
2. 当账号或密码为空、错误或无效时，给出明确提示。
3. 登录成功后进入首页，并按角色加载允许访问的菜单。

**用户与权限页**

1. 提供用户列表、用户表单和角色权限配置区。
2. 用户信息支持配置用户名、密码、角色和状态。
3. 角色配置支持按菜单分组和操作权限分组进行勾选。

### 6.1.4 关键业务规则

1. 管理员角色必须保留“用户与权限”菜单和用户/角色管理权限。
2. 普通角色不能分配管理员专属菜单和管理类操作权限。
3. 若当前用户已登录但没有任何可访问菜单，前端会执行退出并回到登录页。
4. 前端按固定周期同步 `/auth/profile`，角色调整后现有会话能自动感知权限变化。

## 6.2 首页仪表盘模块

### 6.2.1 功能目标

首页用于集中展示平台当前运行状态和电商核心指标，并作为各业务模块的快捷入口。

### 6.2.2 主要展示内容

| 展示区域 | 当前展示内容 |
| --- | --- |
| 统计卡片 | 数据集总数、数据源总数、调度任务数、最近失败任务数、近7天订单量、近7天销售额、低库存商品数、任务成功率 |
| 趋势图 | 近7天订单量与销售额双折线趋势 |
| 排行信息 | 热销商品 Top5 |
| 状态信息 | 最近任务列表、最近执行日志 |
| 快捷入口 | 开始电商数据接入、打开查询分析、执行治理流程、查看任务调度、查看日志详情 |

### 6.2.3 页面交互设计

1. 首页每 15 秒自动刷新一次聚合数据。
2. 统计卡片支持点击跳转到对应模块页面。
3. 核心图表支持跳转到查询分析等业务页面。
4. 最近任务和最近日志用于辅助定位系统当前执行状态。
5. 当没有日志或任务数据时，页面提供空态展示，不阻塞首页加载。

### 6.2.4 当前实现说明

当前首页重点体现模块级跳转和状态总览，不包含独立“最近导入数据集展示”专区，也不实现图表点位级深链分析。

## 6.3 数据源管理模块

### 6.3.1 功能目标

负责维护文件型和数据库型数据源配置，为后续导入提供统一的数据来源入口。

### 6.3.2 主要功能

1. 支持新增、编辑、删除数据源。
2. 支持 `FILE` 与 `MYSQL` 两类数据源。
3. 支持测试连接和启用/停用。
4. 支持同名查重和 `MYSQL` 重复连接处理策略。

### 6.3.3 页面设计

**列表字段**

1. 数据源名称
2. 类型
3. 主机
4. 数据库名
5. 状态
6. 描述

**新增/编辑表单**

1. 数据源名称
2. 数据源类型
3. 描述
4. `MYSQL` 场景下的主机、端口、数据库名、用户名、密码
5. `MYSQL` 场景下的重复连接处理策略 `ALLOW / WARN / REJECT`

### 6.3.4 关键业务规则

1. 数据源名称不能为空，且同一平台内不能重复。
2. `FILE` 数据源用于文件导入，无需远程连接测试。
3. `MYSQL` 数据源支持显式测试连接，成功和失败均返回明确提示。
4. 重复连接处理策略仅在保存 `MYSQL` 数据源时生效：
   `ALLOW` 允许保存且不提示。
   `WARN` 允许保存并返回复用提示。
   `REJECT` 发现重复连接时拒绝保存。
5. 数据源停用后不可用于发起新的导入任务。
6. 若已有数据集依赖该数据源，删除操作会被阻止并提示原因。

## 6.4 电商数据接入模块

### 6.4.1 功能目标

负责把电商文件数据和数据库表数据导入平台，并生成数据集、元数据和导入历史。

### 6.4.2 文件上传导入

**页面输入项**

1. 所属数据源
2. 数据集名称
3. 业务域
4. 编码方式
5. 是否首行为表头
6. 上传文件

**当前支持能力**

1. 支持 `CSV / JSON / Excel` 三类文件格式。
2. 文件格式会自动识别，并根据格式启用对应解析选项。
3. 当格式不支持或解析参数非法时，系统返回明确错误提示。

### 6.4.3 数据库表导入

**页面输入项**

1. `MYSQL` 数据源
2. `Schema / Database`
3. 数据库表
4. 目标数据集名称
5. 业务域
6. 描述

**当前支持能力**

1. 当前仅支持从 `MYSQL` 数据源导入数据库表。
2. 选择数据源后自动加载可用 `Schema / Database`。
3. 切换 `Schema / Database` 后，表列表和预览会同步刷新。
4. 支持查看字段结构和样例数据预览。
5. 当前不支持自定义 SQL 导入。

### 6.4.4 导入历史与结果反馈

**导入历史列表字段**

1. 导入编号
2. 数据集名称
3. 业务域
4. 格式
5. 记录数
6. 状态
7. 操作人
8. 创建时间
9. 错误信息

**详情查看内容**

1. 源数据源信息
2. 数据集名称和业务域
3. 来源文件或来源表
4. 导入参数摘要
5. 记录数、状态和错误原因

### 6.4.5 关键业务规则

1. 导入成功后自动生成数据集和基础元数据。
2. 导入失败后会保留导入历史并记录失败原因。
3. 数据源必须处于启用状态才能执行导入。
4. 数据库表导入仅面向管理员开放，对应操作权限为 `import.database`。

## 6.5 数据集管理与元数据管理模块

### 6.5.1 功能目标

负责提供数据集列表、元数据展示、预览筛选、数据导出和删除校验等能力。

### 6.5.2 数据集列表

**筛选条件**

1. 关键字
2. 来源数据源
3. 业务域
4. 状态

**当前页面字段**

1. 数据集名称
2. 业务域
3. 格式
4. 记录数
5. 字段数
6. 状态
7. 创建人

### 6.5.3 元数据展示

系统在导入成功后会自动抽取基础元数据，当前展示字段如下：

1. 字段名
2. 字段类型
3. 是否可为空
4. 样例值

当前系统不提供字段描述、默认值、在线编辑字段说明和关键字段高亮等扩展能力。

### 6.5.4 数据预览与导出

**数据预览**

1. 支持分页浏览预览数据。
2. 支持按字段和关键字进行简单筛选。
3. 支持依据当前选中的元数据列渲染预览表格。

**数据导出**

1. 支持将当前数据集导出为 `CSV / JSON / Excel`。
2. 导出时可带上当前字段筛选和关键字筛选条件。

### 6.5.5 关键业务规则

1. 预览数据采用分页方式返回，避免一次加载过大结果集。
2. 删除数据集前需要二次确认。
3. 若数据集已被治理流程、任务日志等对象引用，删除会被阻止并提示原因。

## 6.6 查询分析模块

### 6.6.1 功能目标

负责提供基础查询、电商指标分析和结果导出能力，是面向业务用户的核心分析入口。

### 6.6.2 条件查询

**页面能力**

1. 选择数据集。
2. 配置多个查询条件。
3. 支持 `AND / OR` 条件组合。
4. 支持排序字段和升降序。
5. 支持分页查看结果。

**当前支持的操作符**

1. `LIKE`
2. `EQ`
3. `GT`
4. `GTE`
5. `LT`
6. `LTE`
7. `BETWEEN`
8. `TIME_RANGE`

### 6.6.3 SQL 查询

**页面能力**

1. 在当前数据集范围内执行单表 `SELECT` 查询。
2. 支持分页显示查询结果。
3. 支持排序字段和排序方向配置。
4. 支持将 SQL 查询结果导出为 `CSV / JSON / Excel`。

**当前边界**

1. 仅支持当前数据集对应物理表。
2. 不支持跨数据集自由联表 SQL。
3. SQL 不合法时会返回明确错误提示。

### 6.6.4 电商指标图表

当前指标分析页支持以下指标：

1. 订单量趋势
2. 销售额趋势
3. 商品销量排行
4. 分类销售占比
5. 库存预警

页面支持时间范围筛选，并支持与当前查询条件联动。当勾选“联动查询条件”后，切换基础查询条件会同步刷新指标结果。

### 6.6.5 查询结果展示与导出

1. 条件查询结果支持结果表格展示。
2. 页面展示记录数、空值数、重复数等摘要信息。
3. 条件查询结果可按当前筛选条件导出为 `CSV / JSON / Excel`。
4. 当查询无结果时，页面提供空态提示。

## 6.7 数据集成模块

### 6.7.1 功能定位

数据集成能力位于“电商查询分析”页中的独立子标签，用于完成两个数据集之间的基础整合操作。

### 6.7.2 主要功能

1. 选择左侧数据集和右侧数据集。
2. 选择集成模式为 `JOIN` 或 `UNION`。
3. 在 `JOIN` 模式下选择左右关联字段。
4. 配置预览结果条数上限。
5. 填写输出数据集名称和输出业务域。
6. 预览集成结果并保存为新数据集。

### 6.7.3 关键业务规则

1. `JOIN` 适用于订单-商品、商品-库存、用户-订单等典型关联场景。
2. `UNION` 适用于结构相近数据集的纵向合并场景。
3. 字段冲突时系统会自动重命名右侧输出字段，例如增加 `right_` 前缀。
4. 集成结果可直接保存为新数据集，并进入数据集管理链路。

## 6.8 数据治理模块

### 6.8.1 功能目标

负责对数据集执行可配置的治理流程，支持算子启停、流程保存、顺序执行和输出新数据集。

### 6.8.2 算子管理

当前系统内置 9 个治理算子：

| 算子名称 | 键值 | 类型 | 当前用途 |
| --- | --- | --- | --- |
| 空值填充 | `NULL_FILL` | `CLEAN` | 将空值替换为指定内容 |
| 重复数据清理 | `DEDUPLICATE` | `DEDUP` | 按指定字段去重 |
| 字段转换 | `FIELD_CONVERT` | `TRANSFORM` | 执行字段清洗与格式转换 |
| 条件过滤 | `FILTER_KEEP` | `FILTER` | 仅保留满足条件的记录 |
| 订单去重 | `ORDER_DEDUP` | `DEDUP` | 按订单主键去重 |
| 金额标准化 | `AMOUNT_NORMALIZE` | `TRANSFORM` | 统一金额格式 |
| 时间标准化 | `TIME_NORMALIZE` | `TRANSFORM` | 统一时间格式 |
| 商品分类标准化 | `CATEGORY_NORMALIZE` | `TRANSFORM` | 规范分类值 |
| 状态标准化 | `STATUS_NORMALIZE` | `TRANSFORM` | 规范订单或支付状态值 |

页面支持查看算子名称、类型、描述和状态，并支持启用/停用。

### 6.8.3 治理流程配置

当前治理流程配置页支持：

1. 选择输入数据集。
2. 填写流程名称和执行名称。
3. 从算子列表中添加多个步骤。
4. 通过 `JSON` 文本输入算子参数。
5. 对步骤执行上移、下移、删除和新增。
6. 保存治理流程模板。

### 6.8.4 执行结果展示

执行治理流程后，当前页面可展示：

1. 输入记录数
2. 输出记录数
3. 异常处理条数
4. 输出数据集名称
5. 执行摘要
6. 失败步骤与失败原因

### 6.8.5 关键业务规则

1. 至少添加一个步骤且参数校验通过后，才允许保存或执行。
2. 已停用算子不可用于新建流程，也不可参与执行。
3. 治理按步骤顺序执行，不提供复杂拖拽式编排。
4. 治理结果会保存为新数据集，供后续查询、导出或调度使用。

## 6.9 任务调度管理模块

### 6.9.1 功能目标

负责将导入记录和治理流程配置为可重复执行的任务，并提供自动调度和手动触发能力。

### 6.9.2 当前任务模型

当前系统任务类型固定为两类：

1. `IMPORT`
2. `GOVERNANCE`

任务页面支持填写以下内容：

1. 任务名称
2. 任务类型
3. 执行对象
4. `Cron` 表达式
5. 重试策略
6. 描述
7. 状态

### 6.9.3 页面设计

1. 支持新建、编辑和删除任务。
2. 支持启用、暂停、恢复和立即执行。
3. 支持根据任务类型切换可选目标对象：
   `IMPORT` 选择导入历史。
   `GOVERNANCE` 选择治理流程。
4. 内置“每日订单导入任务”“订单自动治理任务”“商品销量统计任务”“库存预警统计任务”等模板名称，便于课程演示。
5. 页面支持自动刷新、筛选条件记忆和常用筛选视图保存。

### 6.9.4 关键业务规则

1. 暂停中的任务不会被自动调度触发。
2. `Cron` 表达式必须合法，保存时会执行语法校验。
3. 到期任务由应用内轮询调度器触发，不依赖外部调度平台。
4. 每次执行都会写入任务日志，用于后续查看和回放。

## 6.10 日志监控模块

### 6.10.1 功能目标

负责记录和展示导入、治理和调度任务的执行过程，为问题定位、结果留档和失败回放提供依据。

### 6.10.2 列表与筛选

**当前日志列表字段**

1. 任务名称
2. 任务类型
3. 操作人
4. 状态
5. 开始时间
6. 结束时间
7. 耗时
8. 执行摘要

**当前筛选能力**

1. 关键字
2. 状态
3. 任务类型
4. 操作人
5. 时间范围
6. 执行目标
7. 近 7 天失败记录快捷筛选

### 6.10.3 详情查看与回放

日志详情区域内可查看以下结构化信息：

1. 输入参数
2. 执行步骤
3. 输出摘要
4. 错误信息
5. 失败原因对象

对于带有调度任务上下文的失败日志，系统支持一键回放。

### 6.10.4 统计与辅助能力

1. 页面会统计成功、失败、运行中和最近失败日志数量。
2. 页面会聚合展示失败原因 Top 分布。
3. 日志页支持保存常用筛选视图。
4. 日志页支持导出当前筛选结果。

### 6.10.5 关键业务规则

1. 仅具备 `log.replay` 权限的用户可执行失败回放。
2. 非调度产生的日志不支持回放。
3. 服务端筛选用于控制日志查询范围，前端提供执行目标和最近失败等补充筛选。

## 6.11 数据导出模块

### 6.11.1 功能目标

负责输出数据集、查询结果和日志结果，满足课程项目中的结果留档、数据核查和演示展示需求。

### 6.11.2 当前导出场景

| 导出场景 | 当前对象 | 支持格式 | 权限要求 |
| --- | --- | --- | --- |
| 数据集导出 | 数据集管理页中的当前数据集 | `CSV / JSON / Excel` | `dataset.export` |
| 条件查询导出 | 查询分析页条件查询结果 | `CSV / JSON / Excel` | `query.export` |
| SQL 查询导出 | 查询分析页 SQL 查询结果 | `CSV / JSON / Excel` | `query.export` |
| 日志导出 | 日志页当前筛选结果 | `CSV / JSON` | `log.export` |
| 治理结果导出 | 通过治理输出数据集进行导出 | `CSV / JSON / Excel` | `dataset.export` |

### 6.11.3 关键业务规则

1. 导出文件名会包含数据集名称或任务名称以及时间戳。
2. 条件查询导出会按当前筛选条件导出命中结果，而不是仅导出当前分页。
3. 当结果规模较大时，系统通过筛选、分页和预览上限控制交互范围。
4. 治理结果当前不提供独立文件导出入口，而是通过输出数据集复用数据集导出能力。

# 7. 页面清单设计

| 序号 | 页面名称 | 路由 | 页面定位 | 关键区域 |
| --- | --- | --- | --- | --- |
| 1 | 登录页 | `/login` | 平台登录入口 | 用户名、密码、登录按钮、错误提示 |
| 2 | 电商仪表盘 | `/dashboard` | 首页总览和快捷入口 | 统计卡片、趋势图、热销商品、最近任务、最近日志 |
| 3 | 数据源管理页 | `/data-sources` | 数据源维护 | 数据源列表、表单弹窗、测试连接、启停 |
| 4 | 电商数据接入页 | `/imports` | 文件和数据库导入入口 | 文件导入区、数据库表导入区、导入历史区 |
| 5 | 电商数据集管理页 | `/datasets` | 数据集、元数据和预览管理 | 数据集列表、元数据表、预览表、导出与删除操作 |
| 6 | 电商查询分析页 | `/queries` | 查询分析统一入口 | 条件/SQL 查询、电商指标子页、数据集成子页 |
| 7 | 电商数据治理页 | `/governance` | 算子管理和治理执行 | 算子列表、流程配置、执行结果 |
| 8 | 电商任务调度页 | `/tasks` | 调度任务管理 | 任务筛选区、任务列表、任务表单、最近日志 |
| 9 | 任务与日志监控页 | `/logs` | 执行日志监控与回放 | 日志筛选区、日志列表、日志详情、导出与回放 |
| 10 | 用户与权限页 | `/users` | 用户和角色权限管理 | 用户列表、用户表单、角色权限配置 |

# 8. 核心数据对象设计

## 8.1 角色对象（`sys_role`）

| 字段 | 说明 |
| --- | --- |
| `role_id` | 角色主键 |
| `role_name` | 角色名称，如 `ADMIN`、`OPERATOR` |
| `role_desc` | 角色说明 |
| `menu_permissions` | 菜单权限序列化结果 |
| `action_permissions` | 操作权限序列化结果 |
| `create_time` | 创建时间 |

## 8.2 用户对象（`sys_user`）

| 字段 | 说明 |
| --- | --- |
| `user_id` | 用户主键 |
| `username` | 登录用户名 |
| `password` | 登录密码的加密存储结果 |
| `role_id` | 角色主键 |
| `status` | 用户状态，当前支持 `ENABLED / DISABLED` |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |

## 8.3 数据源对象（`data_source`）

| 字段 | 说明 |
| --- | --- |
| `source_id` | 数据源主键 |
| `source_name` | 数据源名称 |
| `source_type` | 数据源类型，当前支持 `FILE / MYSQL` |
| `host` | 数据库主机 |
| `port` | 数据库端口 |
| `db_name` | 数据库名 |
| `username` | 数据库用户名 |
| `password` | 数据库连接密码，仅后端使用，列表不回显 |
| `status` | 数据源状态 |
| `description` | 数据源描述 |
| `create_user` | 创建人 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |

说明：`duplicateConnectionStrategy` 是保存数据源时的输入策略，不是当前数据库表中的独立持久化字段。

## 8.4 导入记录对象（`import_record`）

| 字段 | 说明 |
| --- | --- |
| `import_id` | 导入记录主键 |
| `source_id` | 来源数据源 |
| `dataset_name` | 目标数据集名称 |
| `business_domain` | 业务域 |
| `format_type` | 导入格式 |
| `original_file_name` | 来源文件名或来源对象名 |
| `file_path` | 存储路径或来源标识 |
| `status` | 导入状态 |
| `record_count` | 导入记录数 |
| `error_message` | 失败原因 |
| `create_user` | 操作人 |
| `create_time` | 创建时间 |

## 8.5 数据集对象（`data_set`）

| 字段 | 说明 |
| --- | --- |
| `dataset_id` | 数据集主键 |
| `dataset_name` | 数据集名称 |
| `business_domain` | 业务域 |
| `source_id` | 来源数据源 |
| `format_type` | 数据格式 |
| `record_count` | 记录数 |
| `field_count` | 字段数 |
| `storage_path` | 原始存储路径 |
| `physical_table_name` | 平台内部物理表名 |
| `description` | 数据集描述 |
| `creator` | 创建人 |
| `status` | 数据集状态 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |

## 8.6 元数据字段对象（`meta_field`）

| 字段 | 说明 |
| --- | --- |
| `field_id` | 字段主键 |
| `dataset_id` | 所属数据集 |
| `field_name` | 展示字段名 |
| `physical_column_name` | 物理列名 |
| `field_type` | 字段类型 |
| `nullable` | 是否可为空 |
| `sample_value` | 样例值 |
| `field_order` | 字段顺序 |
| `create_time` | 创建时间 |

## 8.7 治理算子对象（`operator_def`）

| 字段 | 说明 |
| --- | --- |
| `operator_id` | 算子主键 |
| `operator_name` | 算子名称 |
| `operator_key` | 算子唯一键值 |
| `operator_type` | 算子类型 |
| `config_schema` | 参数结构说明 |
| `description` | 算子描述 |
| `status` | 启用状态 |
| `create_time` | 创建时间 |

## 8.8 治理流程对象（`governance_flow`）

| 字段 | 说明 |
| --- | --- |
| `flow_id` | 流程主键 |
| `flow_name` | 流程名称 |
| `input_dataset_id` | 输入数据集 |
| `operator_chain` | 算子链配置，包含步骤和参数 |
| `output_dataset_id` | 最近一次输出数据集 |
| `creator` | 创建人 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |

## 8.9 调度任务对象（`task_def`）

| 字段 | 说明 |
| --- | --- |
| `task_id` | 任务主键 |
| `task_name` | 任务名称 |
| `task_type` | 任务类型，当前为 `IMPORT / GOVERNANCE` |
| `target_id` | 目标对象主键 |
| `cron_expr` | `Cron` 表达式 |
| `status` | 任务状态 |
| `retry_policy` | 重试次数 |
| `create_user` | 创建人 |
| `payload` | 扩展载荷 |
| `description` | 描述信息 |
| `next_run_time` | 下次执行时间 |
| `last_run_time` | 上次执行时间 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |

## 8.10 任务日志对象（`task_log`）

| 字段 | 说明 |
| --- | --- |
| `log_id` | 日志主键 |
| `task_id` | 所属任务 |
| `task_type` | 任务类型 |
| `target_id` | 执行目标 |
| `start_time` | 开始时间 |
| `end_time` | 结束时间 |
| `status` | 执行状态 |
| `execution_summary` | 执行摘要 |
| `error_message` | 错误信息 |
| `input_params` | 输入参数 JSON |
| `execution_steps` | 执行步骤 JSON |
| `failure_reason` | 失败原因 JSON |
| `duration` | 耗时 |
| `operator_user` | 操作人 |
| `create_time` | 创建时间 |

## 8.11 课程演示推荐业务字段

平台对业务数据字段不做固定表结构约束，但当前课程演示建议优先采用下列字段组合，以便与查询分析、治理和图表功能保持一致：

| 业务域 | 推荐字段 |
| --- | --- |
| `USER` | `user_id`、`username`、`phone`、`register_time` |
| `PRODUCT` | `product_id`、`product_name`、`category`、`price`、`status` |
| `TRADE` | `order_id`、`user_id`、`product_id`、`quantity`、`amount`、`order_status`、`order_time` |
| `PAYMENT` | `payment_id`、`order_id`、`payment_method`、`payment_status`、`payment_time` |
| `INVENTORY` | `product_id`、`warehouse_id`、`stock_num`、`update_time` |
| `REVIEW` | `review_id`、`user_id`、`product_id`、`score`、`content`、`review_time` |
| `BEHAVIOR_LOG` | `log_id`、`user_id`、`product_id`、`action_type`、`action_time`、`source_channel` |

# 9. 非功能需求设计

## 9.1 性能要求

1. 常规页面加载和切换应保持流畅。
2. 数据预览、条件查询和 SQL 查询采用分页方式返回结果。
3. 文件导入应支持课程项目范围内的中小规模数据文件。
4. 图表统计和聚合查询应在可接受时间内返回结果。
5. 调度轮询与首页自动刷新默认采用 15 秒周期，兼顾实时性和系统负载。

## 9.2 可用性要求

1. 页面导航清晰，模块名称与业务含义保持一致。
2. 关键操作提供确认提示和成功/失败反馈。
3. 空数据、无结果和错误场景提供明确提示。
4. 筛选条件和常用视图支持记忆，便于反复演示和回归测试。

## 9.3 可扩展性要求

1. 支持继续扩展新的业务域枚举。
2. 支持继续扩展新的治理算子和治理模板。
3. 支持在现有查询分析框架下增加新的电商指标图表。
4. 支持在现有权限模型下扩展新的菜单和操作权限。

## 9.4 安全性要求

1. 系统采用登录鉴权和接口权限控制。
2. 菜单权限和操作权限双重生效。
3. 用户密码采用加密存储。
4. 数据源密码不在列表接口中直接回显。
5. 导出、回放、数据源管理和数据库导入均受操作权限控制。

## 9.5 可维护性要求

1. 前后端模块边界清晰，页面与接口职责明确。
2. 数据库表结构、接口清单、用户手册和测试资料齐全。
3. 日志对象采用结构化字段存储，便于定位问题。
4. 导入、治理、调度和日志链路可通过同一套业务对象追踪。

# 10. 技术落地建议

## 10.1 前端实现

当前前端采用以下技术组合：

1. `Vue 3`
2. `TypeScript`
3. `Element Plus`
4. `Pinia`
5. `Vue Router`
6. `ECharts`

该组合用于承载页面路由、状态管理、表单交互、表格展示和图表分析。

## 10.2 后端实现

当前后端核心实现采用以下技术：

1. `Spring Boot`
2. `Spring Security + JWT`
3. `JdbcTemplate`
4. `Spring Validation`
5. `@Scheduled + CronExpression`

其中，登录鉴权、数据源管理、数据接入、查询分析、治理执行、任务管理和日志服务均已基于当前后端工程落地。

## 10.3 数据存储与文件存储

当前项目采用以下存储方式：

1. `H2 / MySQL` 用于存储用户、角色、数据源、导入记录、数据集、元数据、治理流程、任务和日志。
2. 本地文件系统 `storage/` 用于保存上传原文件。
3. 导入后数据以数据集物理表形式落入平台数据库，供查询、治理和调度复用。

## 10.4 电商治理引擎实现

当前治理引擎采用“算子链顺序执行”的实现方式：

1. 输入：指定数据集和治理步骤配置。
2. 处理：按步骤顺序执行算子逻辑。
3. 输出：返回治理统计信息并生成输出数据集。

该实现适合课程项目场景，结构清晰，便于扩展新的治理算子。

## 10.5 调度实现

当前调度采用应用内轮询方式：

1. 任务定义保存 `Cron` 表达式、目标对象和状态。
2. 系统通过定时轮询扫描到期任务。
3. 到期任务交由任务服务执行，并记录结构化日志。

该方案满足课程项目的自动化调度和演示需要，不依赖外部调度平台。

# 11. 项目实施优先级设计

## 11.1 已完成核心功能（MVP）

本轮课程交付已经完成以下核心能力：

1. 登录与权限控制
2. 电商仪表盘
3. 数据源管理
4. 文件导入
5. 数据库表导入
6. 数据集管理与元数据展示
7. 数据预览与导出
8. 条件查询与单表 SQL 查询
9. 电商指标图表分析
10. 数据集成
11. 数据治理
12. 任务调度
13. 日志监控与失败回放
14. 用户与角色权限管理

## 11.2 后续可增强方向

后续若继续扩展，可围绕以下方向演进：

1. 更复杂的图表联动和下钻分析
2. 更多电商治理算子与模板
3. 更大规模数据下的性能优化
4. 更多调度策略和执行告警方式
5. 更细粒度的组织和租户权限控制

# 12. 团队分工设计（9人）

## 12.1 产品经理（1人）

负责以下工作：

1. 电商业务需求梳理
2. 文档结构设计与模块边界定义
3. 页面原型与交付资料整理
4. 课程汇报和演示脚本准备

## 12.2 前端开发（3人）

### 前端1：系统框架与权限

1. 登录页
2. 布局、菜单与路由控制
3. 权限态同步
4. 首页仪表盘

### 前端2：数据接入与数据集管理

1. 数据源管理页
2. 电商数据接入页
3. 数据集管理页
4. 元数据与预览区域

### 前端3：分析、治理与调度

1. 查询分析页
2. 数据集成子标签
3. 数据治理页
4. 任务调度页
5. 日志监控页
6. 用户与权限页

## 12.3 后端开发（5人）

### 后端1：认证与权限

1. 登录鉴权
2. 用户与角色接口
3. 菜单和操作权限控制

### 后端2：数据源与数据接入

1. 数据源管理
2. 文件导入
3. 数据库表导入
4. 导入历史记录

### 后端3：数据集与查询

1. 数据集管理
2. 元数据抽取
3. 数据预览
4. 条件查询与 SQL 查询

### 后端4：治理与集成

1. 数据集成
2. 治理算子
3. 治理流程执行
4. 输出数据集生成

### 后端5：调度与日志

1. 任务调度
2. 任务执行
3. 日志详情
4. 日志回放

# 13. 开发阶段规划

## 13.1 第一阶段：需求分析与结构设计

已完成输出：

1. 电商主题需求梳理
2. 用户故事文档
3. 产品功能设计文档
4. 数据库初步设计

## 13.2 第二阶段：基础框架搭建

已完成输出：

1. 前后端工程初始化
2. 登录与权限基础链路
3. 路由、菜单和公共布局

## 13.3 第三阶段：核心业务功能开发

已完成输出：

1. 数据接入
2. 数据集管理
3. 查询分析
4. 数据治理
5. 任务调度
6. 日志监控

## 13.4 第四阶段：联调与测试

已完成输出：

1. 接口联调
2. 页面回归
3. MySQL 端到端链路补测
4. 文档冻结审计

## 13.5 第五阶段：课程交付准备

当前输出包括：

1. README
2. 用户手册
3. API 文档
4. 测试计划与测试报告
5. 演示脚本与回归检查单

# 14. 风险与应对方案

## 风险1：电商数据来源异构，导入差异较大

应对方案：

1. 文件导入支持 `CSV / JSON / Excel` 三类常用格式。
2. 数据库导入限定为 `MYSQL` 表导入，降低实现复杂度。
3. 通过导入历史保留失败原因，便于快速定位问题。

## 风险2：治理能力容易写成规划态，偏离实际交付

应对方案：

1. 只写当前已实现的算子和执行方式。
2. 采用顺序执行和 `JSON` 参数配置，保持能力可演示、可验证。
3. 通过输出数据集和执行日志证明治理结果。

## 风险3：权限调整后前后端状态不一致

应对方案：

1. 角色权限统一由后端返回。
2. 前端定时同步用户 profile。
3. 菜单和按钮同时基于权限控制，保证界面与接口行为一致。

## 风险4：调度与日志链路不完整，影响课程演示

应对方案：

1. 任务与日志采用统一对象设计。
2. 任务页支持立即执行，便于现场触发。
3. 日志页支持查看详情、导出结果和失败回放，形成完整演示闭环。

# 15. 项目亮点总结

1. 主题明确，围绕电商订单、商品和库存主链路展开，业务场景清晰。
2. 主链路完整，覆盖接入、管理、分析、治理、调度和日志监控。
3. 权限模型真实可用，支持菜单权限、操作权限和现有会话自动同步。
4. 数据接入能力完整，既支持多格式文件导入，也支持 `MYSQL` 数据库表导入。
5. 查询分析能力较完整，支持多条件组合、单表 SQL、电商指标图表和数据集成子标签。
6. 治理设计工程化，支持算子启停、流程保存、步骤重排和结构化结果输出。
7. 调度与日志链路可闭环，支持立即执行、失败回放、日志导出和筛选视图保存。
8. 文档与实现对齐，适合作为课程项目交付材料直接提交和演示。

# 16. 可直接用于课堂汇报的项目说明

本项目实现的是一个面向电商业务的数据湖管理平台 Web 系统。平台以订单、商品、库存为主线，支持多源数据接入、数据集管理、元数据展示、查询分析、数据集成、数据治理、任务调度和日志监控等功能。系统采用真实前后端联动实现，具备角色权限控制、导入历史追踪、图表分析、治理输出和失败日志回放能力，能够完整展示电商数据平台从接入到分析再到自动化处理的业务闭环。

# 17. 答辩时推荐的一句话定位

我们做的是一个面向电商业务的数据湖管理平台，重点解决多源数据的统一接入、查询分析、治理加工和自动调度问题，并通过 Web 端实现可视化管理和课程演示闭环。
