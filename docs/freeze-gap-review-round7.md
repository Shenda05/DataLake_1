# 第七轮冻结前差距复审（Round 7，Round 8 收口更新）

更新时间：`2026-04-04`
复审基线：当前仓库代码 + [README.md](/Users/xyd/Desktop/DataLake/README.md) + [api-contract.md](/Users/xyd/Desktop/DataLake/docs/api-contract.md) + [architecture.md](/Users/xyd/Desktop/DataLake/docs/architecture.md) + [user-manual.md](/Users/xyd/Desktop/DataLake/docs/user-manual.md) + 电商版产品功能设计文档 / 用户故事文档 / 迭代规划。

## 复审结论

- 主流程已闭环：登录 → 电商数据接入 → 数据集管理 → 查询分析与数据集成 → 数据治理 → 任务调度 → 日志监控。
- 自动化资产与人工资产均已补齐：`regression-suite` 已通过，浏览器检查单已于 `2026-04-04` 人工执行通过。
- 当前已无冻结阻断级缺陷；剩余差异主要属于 MVP 边界，需要在课程文档中说明，而不是继续扩功能。
- Round 8 的重点已经从“补功能”切换为“统一口径、统一资产、统一演示路径”。

## 一、必须修（当前未关闭项）

当前无未关闭的“必须修”项。

## 二、已闭环事项（本轮已收口）

| ID | 已闭环项 | 影响范围 | 当前状态 |
| --- | --- | --- | --- |
| C1 | 接口文档与真实实现漂移 | 答辩、接口设计章节 | 已更新 `api-contract / api-inventory`，与 Controller 清单对齐 |
| C2 | 架构/任务类型口径错误 | 架构章节、问答口径 | 已明确仅 `IMPORT / GOVERNANCE`，数据集成为查询页子标签能力 |
| C3 | 测试计划、用户手册、演示脚本滞后 | 验收、操作复现、PPT 讲解 | 已改为电商语义与当前页面行为 |
| C4 | 回归脚本边界不清晰 | 冻结验收与复测效率 | 已通过 `scripts/README.md` 与 `regression-coverage-matrix.md` 明确职责边界 |
| C5 | 运行说明带作者本机路径 | 异机复现、课程检查 | 已统一为 `JDK 17+` 通用启动说明 |
| C6 | 浏览器人工检查状态未同步到冻结材料 | 冻结状态、测试附录 | 已更新为 `2026-04-04` 人工通过，可复查 |
| C7 | 最新能力未同步文档资产 | 交付完整性 | 已补充数据源重复策略、数据库 schema 自动识别下拉、最新人工检查状态 |

## 三、记录到文档说明（MVP 边界，不追加开发）

| ID | 内容 | 风险等级 | 当前实现口径 | 建议写入位置 |
| --- | --- | --- | --- | --- |
| D1 | 数据集成不是一级菜单 | 中 | 当前通过“查询分析”页子标签提供 `JOIN / UNION` 与保存结果能力 | README / 架构文档 / 用户手册 |
| D2 | 无独立“系统设置”模块 | 低 | 本期以内嵌配置与固定参数为主，不单独做后台设置中心 | 冻结说明 / 后续规划 |
| D3 | 数据库导入不支持“自定义 SQL 导入” | 中 | 当前仅支持选择 `schema/database + table` 导入 | 产品偏差说明 / 用户手册 |
| D4 | 首页跳转只做到模块级，不做图表点位级深链 | 低 | 当前统计卡片和核心图表已支持跳转到对应模块页，但不携带某个柱点/折线点的筛选条件 | 用户手册 / 答辩说明 |
| D5 | 治理结果无独立导出按钮 | 低 | 当前通过“输出数据集”进入数据集页后再导出 | 用户手册 / 测试说明 |
| D6 | 数据源测试连接不维护“已测试通过”持久状态 | 中 | 测试连接是显式动作；平台不记录持久测试标记，仅返回即时结果 | 用户故事偏差说明 / 用户手册 |
| D7 | SQL 查询仍限定单数据集单表 | 中 | 跨表场景通过数据集成子标签处理，不在 SQL 直接放开多表 | API 合同 / 用户手册 |
| D8 | 调度仍是应用内轮询，不是 Quartz | 中 | 满足课程演示与任务触发闭环，不做独立调度器重构 | README / 架构文档 |

## 四、可忽略（不阻断课程验收）

| ID | 问题 | 风险等级 | 说明 |
| --- | --- | --- | --- |
| I1 | 前端生产构建仍有 chunk 体积告警 | 低 | 不影响运行、演示与答辩 |
| I2 | 少量增强型注释仍保留“后续优化”口吻 | 低 | 不影响功能闭环，已避免与当前实现冲突 |
| I3 | 部分页面仍保留通用平台骨架用语 | 低 | 不影响课程项目的电商化主表达 |

## 五、冻结收口检查单

- [x] 接口、架构、测试、用户手册、README 与代码实现一致。
- [x] `scripts/` 与 `docs/` 的职责边界清晰，统一回归入口可直接执行。
- [x] 演示数据可通过 `bash scripts/ecommerce-demo-smoke.sh all` 快速准备。
- [x] 后端编译、前端构建、`regression-suite` 自动化回归均通过。
- [x] 浏览器人工检查单已于 `2026-04-04` 执行通过。
- [x] 冻结说明、接口清单、表结构清单、模块说明、演示 Runbook 均可直接用于课程文档附录。
