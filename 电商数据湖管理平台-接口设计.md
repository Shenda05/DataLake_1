**电商数据湖管理平台**

**接口设计文档**

API Specification

项目名称：电商数据湖管理平台（Web 端）

摘要：本文档依据当前仓库冻结版本的实际实现，对电商数据湖管理平台后端接口进行说明。文档面向课程项目报告与前后端联调场景，重点覆盖登录权限、仪表盘、数据源管理、数据接入、数据集管理、查询分析、数据治理、任务调度、日志监控与导出接口等内容。所有接口均以当前代码实现为准，不扩写未实现能力。

相关文档：

1. 《电商数据湖管理平台-产品功能设计文档-V3》
2. 《电商数据湖管理平台-用户故事文档-V2》
3. `docs/api-inventory.md`
4. `docs/api-contract.md`
5. `docs/module-map.md`
6. `docs/freeze-release-note.md`
7. `frontend/src/api/platform.ts`
8. 后端 `Controller` 与请求 DTO

版权所有：除特别允许外，仅供项目组内部教学、课程开发和课程报告使用。

**修改记录：**

| 日期 | 版本 | 说明 | 作者 |
| --- | --- | --- | --- |
| 2026/04/05 | V1.0 | 依据教师模板并结合当前冻结代码实现，新增接口设计文档 | 肖溢丹 |

# 目录

| 章节 | 内容 |
| --- | --- |
| 第1章 | 简介 |
| 第2章 | 接口列表 |

# 1 简介

## 1.1 目的

本文档用于说明电商数据湖管理平台当前版本的接口设计方案，重点回答以下问题：

1. 当前系统实际开放了哪些 API 接口。
2. 各模块接口的地址、请求方式、权限要求和参数结构是什么。
3. 各接口返回的数据结构、分页结构和错误处理方式是什么。
4. 哪些接口返回统一 JSON，哪些接口返回文件流。
5. 当前实现边界在哪里，哪些能力并未实现为接口。

本文档以课程项目交付为目标，强调“与代码实现一致”，不对未落地的注册、邮件验证、刷新 token、日志导出、流程删除等能力进行虚构设计。

## 1.2 范围

当前接口设计覆盖后端 `11` 个控制器中的 `58` 个实际接口，按业务分为以下模块：

1. 登录与权限
2. 仪表盘
3. 数据源管理
4. 数据接入
5. 数据集管理
6. 查询分析
7. 数据治理
8. 任务调度
9. 日志监控
10. 导出接口

接口事实源优先级如下：

1. 后端 `Controller` 路由与请求 DTO
2. 前端 `frontend/src/api/platform.ts` 类型定义
3. `docs/api-inventory.md`
4. `docs/api-contract.md`

# 2 接口列表

## 2.1 接口统一约定

### 2.1.1 基础路径

- 接口基础路径：`/api`
- 当前前端默认访问地址：`http://localhost:8080/api`

### 2.1.2 鉴权方式

- 当前认证方式为 `JWT`
- 请求头格式：`Authorization: Bearer <token>`
- 无需登录即可访问的接口仅包括：
  - `POST /api/auth/login`
  - `Swagger` 与 `H2 Console` 相关调试入口
- 其他业务接口默认均要求登录后访问

### 2.1.3 统一响应结构

普通业务成功响应和大多数参数错误响应采用统一结构：

```json
{
  "code": 0,
  "message": "OK",
  "data": {},
  "timestamp": "2026-04-05T08:00:00Z",
  "requestId": "req-demo-001"
}
```

字段说明如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `code` | 业务状态码 | `int` |
| `message` | 结果说明 | `string` |
| `data` | 业务数据体 | `object / array / null` |
| `timestamp` | 返回时间 | `string` |
| `requestId` | 请求追踪编号 | `string` |

说明：

1. 普通查询、保存、删除、治理、任务等接口默认返回统一 JSON。
2. 文件导出接口直接返回二进制文件流，不走 `ApiResponse`。
3. 当前 `401/403` 多数由 Spring Security 直接拦截，前端主要按 HTTP 状态码处理。

### 2.1.4 分页结构

分页型接口使用如下统一结构：

```json
{
  "pageNum": 1,
  "pageSize": 20,
  "total": 128,
  "records": []
}
```

字段说明如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `pageNum` | 当前页码 | `int` |
| `pageSize` | 每页条数 | `int` |
| `total` | 总记录数 | `long` |
| `records` | 当前页数据列表 | `array` |

### 2.1.5 文件上传与下载

当前接口涉及两类特殊传输：

1. 文件上传：
   - 接口：`POST /api/imports/file`
   - 请求类型：`multipart/form-data`
2. 文件下载：
   - 接口：
     - `GET /api/datasets/{datasetId}/export`
     - `POST /api/queries/filter/export`
     - `POST /api/queries/sql/export`
   - 返回方式：二进制文件流
   - 支持格式：`csv`、`json`、`xlsx`
   - 文件名规则：`数据集名或导出主题_时间戳.扩展名`

### 2.1.6 公共状态码

| HTTP 状态 | 业务码 | 说明 |
| --- | --- | --- |
| `200` | `0` | 请求成功 |
| `400` | `40001` | 参数校验失败或业务参数不合法 |
| `401` | `40101` | 未登录或 token 无效 |
| `403` | `40301` | 无权限访问 |
| `500` | `50001` | 系统内部异常 |

### 2.1.7 当前关键枚举

| 枚举项 | 当前取值 |
| --- | --- |
| 业务域 `businessDomain` | `USER / PRODUCT / TRADE / PAYMENT / INVENTORY / REVIEW / BEHAVIOR_LOG` |
| 任务类型 `taskType` | `IMPORT / GOVERNANCE` |
| 数据源类型 `sourceType` | `FILE / MYSQL` |
| 常见状态 `status` | `ENABLED / DISABLED / READY / PAUSED / SUCCESS / FAILED` |
| 重复连接策略 `duplicateConnectionStrategy` | `ALLOW / WARN / REJECT` |
| 集成模式 `mode` | `JOIN / UNION` |

## 2.2 登录与权限

### 2.2.1 用户登录

- 接口地址：`POST /api/auth/login`
- 请求方式：`POST`
- 接口描述：用户通过用户名和密码登录系统，登录成功后返回 JWT、角色信息和当前菜单/操作权限。
- 权限要求：无需登录
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `username` | 登录用户名 | 是 | `string` |
| `password` | 登录密码 | 是 | `string` |

**请求示例**

```json
{
  "username": "admin",
  "password": "admin123"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；认证失败 `401/40101`；服务异常 `500/50001`。

**响应参数**

除统一响应字段外，`data` 中包含：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.token` | JWT 令牌 | `string` |
| `data.username` | 当前用户名 | `string` |
| `data.role` | 当前角色 | `string` |
| `data.displayName` | 展示名称 | `string` |
| `data.menus` | 当前菜单权限列表 | `string[]` |
| `data.actions` | 当前操作权限列表 | `string[]` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9.demo",
    "username": "admin",
    "role": "ADMIN",
    "displayName": "admin",
    "menus": ["dashboard", "data-sources", "imports"],
    "actions": ["source.manage", "user.manage", "role.manage"]
  },
  "timestamp": "2026-04-05T08:00:00Z",
  "requestId": "req-login-001"
}
```

### 2.2.2 用户退出

- 接口地址：`POST /api/auth/logout`
- 请求方式：`POST`
- 接口描述：退出登录接口。当前版本中该接口主要用于前端退出流程确认，服务端直接返回成功响应，不维护服务端 token 黑名单。
- 权限要求：已登录用户
- 请求数据类型：无请求体
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
POST /api/auth/logout
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

`data` 为 `null`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": null,
  "timestamp": "2026-04-05T08:02:00Z",
  "requestId": "req-logout-001"
}
```

### 2.2.3 当前用户信息

- 接口地址：`GET /api/auth/profile`
- 请求方式：`GET`
- 接口描述：获取当前登录用户的基础信息、角色以及最新菜单权限和操作权限，用于页面权限同步。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/auth/profile
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.username` | 当前用户名 | `string` |
| `data.role` | 当前角色 | `string` |
| `data.displayName` | 展示名称 | `string` |
| `data.menus` | 当前菜单权限列表 | `string[]` |
| `data.actions` | 当前操作权限列表 | `string[]` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "username": "operator",
    "role": "OPERATOR",
    "displayName": "operator",
    "menus": ["dashboard", "imports", "datasets", "queries"],
    "actions": ["query.export", "task.trigger", "log.replay"]
  },
  "timestamp": "2026-04-05T08:03:00Z",
  "requestId": "req-profile-001"
}
```

### 2.2.4 角色列表

- 接口地址：`GET /api/roles`
- 请求方式：`GET`
- 接口描述：查询当前系统的角色列表及其菜单权限、操作权限。
- 权限要求：`ROLE_ADMIN`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/roles
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`；无权限 `403`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].roleId` | 角色 ID | `long` |
| `data[].roleName` | 角色名称 | `string` |
| `data[].roleDesc` | 角色说明 | `string` |
| `data[].menuPermissions` | 菜单权限列表 | `string[]` |
| `data[].actionPermissions` | 操作权限列表 | `string[]` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "roleId": 1,
      "roleName": "ADMIN",
      "roleDesc": "平台管理员",
      "menuPermissions": ["dashboard", "data-sources", "users"],
      "actionPermissions": ["source.manage", "user.manage", "role.manage"]
    }
  ],
  "timestamp": "2026-04-05T08:04:00Z",
  "requestId": "req-role-list-001"
}
```

### 2.2.5 更新角色

- 接口地址：`PUT /api/roles/{roleId}`
- 请求方式：`PUT`
- 接口描述：更新角色描述、菜单权限和操作权限。
- 权限要求：`ACTION_role.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

路径参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `roleId` | 角色 ID | 是 | `long` |

请求体参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `roleDesc` | 角色说明 | 否 | `string` |
| `menus` | 菜单权限列表 | 是 | `string[]` |
| `actions` | 操作权限列表 | 否 | `string[]` |

**请求示例**

```json
{
  "roleDesc": "数据操作员",
  "menus": ["dashboard", "imports", "datasets", "queries", "governance", "tasks", "logs"],
  "actions": ["query.export", "dataset.export", "task.trigger", "log.replay"]
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.roleId` | 角色 ID | `long` |
| `data.roleName` | 角色名称 | `string` |
| `data.roleDesc` | 角色说明 | `string` |
| `data.menuPermissions` | 菜单权限列表 | `string[]` |
| `data.actionPermissions` | 操作权限列表 | `string[]` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "roleId": 2,
    "roleName": "OPERATOR",
    "roleDesc": "数据操作员",
    "menuPermissions": ["dashboard", "imports", "queries", "logs"],
    "actionPermissions": ["query.export", "log.replay"]
  },
  "timestamp": "2026-04-05T08:05:00Z",
  "requestId": "req-role-update-001"
}
```

### 2.2.6 用户列表

- 接口地址：`GET /api/users`
- 请求方式：`GET`
- 接口描述：查询用户列表。
- 权限要求：`ROLE_ADMIN`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/users
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`；无权限 `403`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].userId` | 用户 ID | `long` |
| `data[].username` | 用户名 | `string` |
| `data[].roleId` | 角色 ID | `long` |
| `data[].role` | 角色名称 | `string` |
| `data[].roleDesc` | 角色说明 | `string` |
| `data[].status` | 用户状态 | `string` |
| `data[].createTime` | 创建时间 | `string` |
| `data[].updateTime` | 更新时间 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "userId": 1,
      "username": "admin",
      "roleId": 1,
      "role": "ADMIN",
      "roleDesc": "平台管理员",
      "status": "ENABLED",
      "createTime": "2026-04-04 12:00:00",
      "updateTime": "2026-04-04 12:00:00"
    }
  ],
  "timestamp": "2026-04-05T08:06:00Z",
  "requestId": "req-user-list-001"
}
```

### 2.2.7 新增用户

- 接口地址：`POST /api/users`
- 请求方式：`POST`
- 接口描述：新增系统用户。
- 权限要求：`ACTION_user.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `username` | 用户名 | 是 | `string` |
| `password` | 密码 | 创建时必填 | `string` |
| `roleId` | 角色 ID | 是 | `long` |
| `status` | 用户状态 | 否 | `string` |

**请求示例**

```json
{
  "username": "analyst01",
  "password": "123456",
  "roleId": 2,
  "status": "ENABLED"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“用户列表”中的用户对象字段一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "userId": 3,
    "username": "analyst01",
    "roleId": 2,
    "role": "OPERATOR",
    "roleDesc": "数据操作员",
    "status": "ENABLED",
    "createTime": "2026-04-05 08:07:00",
    "updateTime": "2026-04-05 08:07:00"
  },
  "timestamp": "2026-04-05T08:07:00Z",
  "requestId": "req-user-create-001"
}
```

### 2.2.8 更新用户

- 接口地址：`PUT /api/users/{userId}`
- 请求方式：`PUT`
- 接口描述：更新用户信息。当前实现中密码为可选，若不传则保持原密码不变。
- 权限要求：`ACTION_user.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

路径参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `userId` | 用户 ID | 是 | `long` |

请求体参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `username` | 用户名 | 是 | `string` |
| `password` | 新密码 | 否 | `string` |
| `roleId` | 角色 ID | 是 | `long` |
| `status` | 用户状态 | 否 | `string` |

**请求示例**

```json
{
  "username": "operator",
  "roleId": 2,
  "status": "ENABLED"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“用户列表”中的用户对象字段一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "userId": 2,
    "username": "operator",
    "roleId": 2,
    "role": "OPERATOR",
    "roleDesc": "数据操作员",
    "status": "ENABLED",
    "createTime": "2026-04-04 12:00:00",
    "updateTime": "2026-04-05 08:08:00"
  },
  "timestamp": "2026-04-05T08:08:00Z",
  "requestId": "req-user-update-001"
}
```

### 2.2.9 删除用户

- 接口地址：`DELETE /api/users/{userId}`
- 请求方式：`DELETE`
- 接口描述：删除指定用户。当前实现禁止删除当前登录用户。
- 权限要求：`ACTION_user.manage`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `userId` | 用户 ID | 是 | `long` |

**请求示例**

```http
DELETE /api/users/3
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

`data` 为 `null`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": null,
  "timestamp": "2026-04-05T08:09:00Z",
  "requestId": "req-user-delete-001"
}
```

## 2.3 仪表盘

### 2.3.1 总览统计

- 接口地址：`GET /api/dashboard/overview`
- 请求方式：`GET`
- 接口描述：返回首页统计卡片所需的总览数据。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/dashboard/overview
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`；服务异常 `500/50001`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.dataSources` | 数据源总数 | `int` |
| `data.datasets` | 数据集总数 | `int` |
| `data.newDatasetsToday` | 今日新增数据集数 | `int` |
| `data.totalTasks` | 任务总数 | `int` |
| `data.runningTasks` | 运行中任务数 | `int` |
| `data.successTasks` | 成功任务数 | `int` |
| `data.failedTasks` | 失败任务数 | `int` |
| `data.recentFailedTasks` | 最近失败任务数 | `int` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "dataSources": 2,
    "datasets": 8,
    "newDatasetsToday": 1,
    "totalTasks": 4,
    "runningTasks": 1,
    "successTasks": 12,
    "failedTasks": 2,
    "recentFailedTasks": 1
  },
  "timestamp": "2026-04-05T08:10:00Z",
  "requestId": "req-dashboard-overview-001"
}
```

### 2.3.2 任务趋势

- 接口地址：`GET /api/dashboard/task-trend`
- 请求方式：`GET`
- 接口描述：返回最近任务执行趋势数据。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/dashboard/task-trend
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].day` | 日期 | `string` |
| `data[].total` | 当日执行总量 | `int` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    { "day": "2026-04-01", "total": 4 },
    { "day": "2026-04-02", "total": 6 }
  ],
  "timestamp": "2026-04-05T08:11:00Z",
  "requestId": "req-dashboard-trend-001"
}
```

### 2.3.3 最近任务

- 接口地址：`GET /api/dashboard/recent-tasks`
- 请求方式：`GET`
- 接口描述：返回首页最近任务列表。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/dashboard/recent-tasks
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].taskId` | 任务 ID | `long` |
| `data[].taskName` | 任务名称 | `string` |
| `data[].taskType` | 任务类型 | `string` |
| `data[].status` | 任务状态 | `string` |
| `data[].nextRunTime` | 下次执行时间 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "taskId": 11,
      "taskName": "每日订单导入任务",
      "taskType": "IMPORT",
      "status": "ENABLED",
      "nextRunTime": "2026-04-06 02:00:00"
    }
  ],
  "timestamp": "2026-04-05T08:12:00Z",
  "requestId": "req-dashboard-recent-001"
}
```

### 2.3.4 电商概览

- 接口地址：`GET /api/dashboard/ecommerce`
- 请求方式：`GET`
- 接口描述：返回订单趋势、销售额趋势、热销商品和低库存统计，用于首页图表展示。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/dashboard/ecommerce
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.orderTrend` | 订单量趋势列表 | `array` |
| `data.salesTrend` | 销售额趋势列表 | `array` |
| `data.topProducts` | 热销商品排行 | `array` |
| `data.lowStockCount` | 低库存商品数 | `long` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "orderTrend": [{ "day": "2026-04-01", "value": 120 }],
    "salesTrend": [{ "day": "2026-04-01", "value": 9800.5 }],
    "topProducts": [{ "name": "无线鼠标", "value": 168 }],
    "lowStockCount": 6
  },
  "timestamp": "2026-04-05T08:13:00Z",
  "requestId": "req-dashboard-ecom-001"
}
```

## 2.4 数据源管理

### 2.4.1 数据源列表

- 接口地址：`GET /api/data-sources`
- 请求方式：`GET`
- 接口描述：查询数据源列表。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/data-sources
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].sourceId` | 数据源 ID | `long` |
| `data[].sourceName` | 数据源名称 | `string` |
| `data[].sourceType` | 数据源类型 | `string` |
| `data[].host` | 主机地址 | `string` |
| `data[].port` | 端口 | `int` |
| `data[].dbName` | 数据库名称 | `string` |
| `data[].username` | 用户名 | `string` |
| `data[].status` | 状态 | `string` |
| `data[].description` | 描述 | `string` |
| `data[].warningMessage` | 预警信息 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "sourceId": 1,
      "sourceName": "电商业务 MySQL",
      "sourceType": "MYSQL",
      "host": "127.0.0.1",
      "port": 3306,
      "dbName": "data_lake_platform",
      "username": "root",
      "status": "ENABLED",
      "description": "订单业务库",
      "warningMessage": null
    }
  ],
  "timestamp": "2026-04-05T08:14:00Z",
  "requestId": "req-source-list-001"
}
```

### 2.4.2 新增数据源

- 接口地址：`POST /api/data-sources`
- 请求方式：`POST`
- 接口描述：新增数据源。当前支持 `FILE` 和 `MYSQL` 两类数据源，`MYSQL` 可选重复连接处理策略。
- 权限要求：`ACTION_source.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceName` | 数据源名称 | 是 | `string` |
| `sourceType` | 数据源类型 | 是 | `string` |
| `host` | 主机地址 | `MYSQL` 时建议填写 | `string` |
| `port` | 端口 | `MYSQL` 时建议填写 | `int` |
| `dbName` | 数据库名 | `MYSQL` 时建议填写 | `string` |
| `username` | 用户名 | 否 | `string` |
| `password` | 密码 | 否 | `string` |
| `description` | 描述 | 否 | `string` |
| `duplicateConnectionStrategy` | 重复连接策略 | 否 | `string` |

**请求示例**

```json
{
  "sourceName": "电商业务 MySQL",
  "sourceType": "MYSQL",
  "host": "127.0.0.1",
  "port": 3306,
  "dbName": "data_lake_platform",
  "username": "root",
  "password": "root",
  "description": "订单业务库",
  "duplicateConnectionStrategy": "WARN"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“数据源列表”中的数据源对象字段一致，可能额外返回 `warningMessage`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "sourceId": 3,
    "sourceName": "电商业务 MySQL",
    "sourceType": "MYSQL",
    "host": "127.0.0.1",
    "port": 3306,
    "dbName": "data_lake_platform",
    "username": "root",
    "status": "ENABLED",
    "description": "订单业务库",
    "warningMessage": "已存在相同 MYSQL 连接配置的数据源，当前按 WARN 策略允许保存"
  },
  "timestamp": "2026-04-05T08:15:00Z",
  "requestId": "req-source-create-001"
}
```

### 2.4.3 更新数据源

- 接口地址：`PUT /api/data-sources/{sourceId}`
- 请求方式：`PUT`
- 接口描述：更新数据源信息。
- 权限要求：`ACTION_source.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

路径参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |

请求体参数与新增数据源一致。

**请求示例**

```json
{
  "sourceName": "电商业务 MySQL",
  "sourceType": "MYSQL",
  "host": "127.0.0.1",
  "port": 3306,
  "dbName": "data_lake_platform",
  "username": "root",
  "password": "root",
  "description": "订单业务库-更新后",
  "duplicateConnectionStrategy": "ALLOW"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“数据源列表”中的数据源对象字段一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "sourceId": 1,
    "sourceName": "电商业务 MySQL",
    "sourceType": "MYSQL",
    "host": "127.0.0.1",
    "port": 3306,
    "dbName": "data_lake_platform",
    "username": "root",
    "status": "ENABLED",
    "description": "订单业务库-更新后",
    "warningMessage": null
  },
  "timestamp": "2026-04-05T08:16:00Z",
  "requestId": "req-source-update-001"
}
```

### 2.4.4 删除数据源

- 接口地址：`DELETE /api/data-sources/{sourceId}`
- 请求方式：`DELETE`
- 接口描述：删除数据源。当前实现中，若该数据源下已有数据集，则禁止删除。
- 权限要求：`ACTION_source.manage`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |

**请求示例**

```http
DELETE /api/data-sources/2
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

`data` 为 `null`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": null,
  "timestamp": "2026-04-05T08:17:00Z",
  "requestId": "req-source-delete-001"
}
```

### 2.4.5 测试连接

- 接口地址：`POST /api/data-sources/{sourceId}/test`
- 请求方式：`POST`
- 接口描述：测试指定数据源连接是否可用。
- 权限要求：`ACTION_source.manage`
- 请求数据类型：无请求体
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |

**请求示例**

```http
POST /api/data-sources/1/test
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.sourceId` | 数据源 ID | `long` |
| `data.connected` | 是否连通 | `boolean` |
| `data.message` | 连接结果说明 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "sourceId": 1,
    "connected": true,
    "message": "连接测试成功"
  },
  "timestamp": "2026-04-05T08:18:00Z",
  "requestId": "req-source-test-001"
}
```

### 2.4.6 更新数据源状态

- 接口地址：`POST /api/data-sources/{sourceId}/status`
- 请求方式：`POST`
- 接口描述：启用或停用数据源。
- 权限要求：`ACTION_source.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

路径参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |

请求体参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `status` | 目标状态，支持 `ENABLED / DISABLED` | 是 | `string` |

**请求示例**

```json
{
  "status": "DISABLED"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“数据源列表”中的数据源对象字段一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "sourceId": 1,
    "sourceName": "电商业务 MySQL",
    "sourceType": "MYSQL",
    "status": "DISABLED",
    "description": "订单业务库",
    "warningMessage": null
  },
  "timestamp": "2026-04-05T08:19:00Z",
  "requestId": "req-source-status-001"
}
```

## 2.5 数据接入

### 2.5.1 文件导入

- 接口地址：`POST /api/imports/file`
- 请求方式：`POST`
- 接口描述：上传文件并导入为数据集。当前支持 `CSV / JSON / EXCEL` 文件。
- 权限要求：已登录用户
- 请求数据类型：`multipart/form-data`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `file` | 上传文件 | 是 | `file` |
| `datasetName` | 数据集名称 | 是 | `string` |
| `businessDomain` | 业务域 | 否 | `string` |
| `sourceId` | 数据源 ID | 是 | `long` |
| `encoding` | 文件编码 | 否 | `string` |
| `headerRow` | 首行是否为表头，默认 `true` | 否 | `boolean` |

**请求示例**

```http
POST /api/imports/file
Content-Type: multipart/form-data

file=orders.csv
datasetName=订单明细数据
businessDomain=TRADE
sourceId=2
encoding=UTF-8
headerRow=true
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；服务异常 `500/50001`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.importId` | 导入记录 ID | `long` |
| `data.datasetId` | 生成的数据集 ID | `long` |
| `data.datasetName` | 数据集名称 | `string` |
| `data.businessDomain` | 业务域 | `string` |
| `data.formatType` | 文件类型 | `string` |
| `data.recordCount` | 导入记录数 | `int` |
| `data.status` | 导入状态 | `string` |
| `data.errorMessage` | 错误信息 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "importId": 1001,
    "datasetId": 2001,
    "datasetName": "订单明细数据",
    "businessDomain": "TRADE",
    "formatType": "CSV",
    "recordCount": 1280,
    "status": "SUCCESS",
    "errorMessage": null
  },
  "timestamp": "2026-04-05T08:20:00Z",
  "requestId": "req-import-file-001"
}
```

### 2.5.2 数据库表导入

- 接口地址：`POST /api/imports/database`
- 请求方式：`POST`
- 接口描述：将 `MYSQL` 数据库中的指定表导入为数据集。
- 权限要求：`ACTION_import.database`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |
| `schemaName` | Schema / Database 名称 | 否 | `string` |
| `tableName` | 表名 | 是 | `string` |
| `datasetName` | 输出数据集名称 | 是 | `string` |
| `businessDomain` | 业务域 | 否 | `string` |
| `description` | 数据集描述 | 否 | `string` |

**请求示例**

```json
{
  "sourceId": 1,
  "schemaName": "data_lake_platform",
  "tableName": "orders",
  "datasetName": "订单业务表快照",
  "businessDomain": "TRADE",
  "description": "数据库表导入"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`；服务异常 `500/50001`。

**响应参数**

与“文件导入”返回结构一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "importId": 1002,
    "datasetId": 2002,
    "datasetName": "订单业务表快照",
    "businessDomain": "TRADE",
    "formatType": "MYSQL_TABLE",
    "recordCount": 560,
    "status": "SUCCESS",
    "errorMessage": null
  },
  "timestamp": "2026-04-05T08:21:00Z",
  "requestId": "req-import-db-001"
}
```

### 2.5.3 数据库 Schema 列表

- 接口地址：`GET /api/imports/database/schemas`
- 请求方式：`GET`
- 接口描述：获取指定 `MYSQL` 数据源可选的 `Schema / Database` 列表。
- 权限要求：`ACTION_import.database`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |

**请求示例**

```http
GET /api/imports/database/schemas?sourceId=1
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

`data` 为 `string[]`，每个元素表示一个可选 `Schema / Database` 名称。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": ["data_lake_platform", "ecommerce_demo"],
  "timestamp": "2026-04-05T08:22:00Z",
  "requestId": "req-import-schemas-001"
}
```

### 2.5.4 数据库表列表

- 接口地址：`GET /api/imports/database/tables`
- 请求方式：`GET`
- 接口描述：获取指定数据源和 Schema 下的表列表。
- 权限要求：`ACTION_import.database`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |
| `schemaName` | Schema / Database 名称 | 否 | `string` |

**请求示例**

```http
GET /api/imports/database/tables?sourceId=1&schemaName=data_lake_platform
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].schemaName` | Schema 名称 | `string` |
| `data[].tableName` | 表名 | `string` |
| `data[].displayName` | 展示名称 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "schemaName": "data_lake_platform",
      "tableName": "orders",
      "displayName": "data_lake_platform.orders"
    }
  ],
  "timestamp": "2026-04-05T08:23:00Z",
  "requestId": "req-import-tables-001"
}
```

### 2.5.5 数据库表预览

- 接口地址：`GET /api/imports/database/preview`
- 请求方式：`GET`
- 接口描述：预览数据库表结构和样例数据。当前 `limit` 默认 `10`，最大 `50`。
- 权限要求：`ACTION_import.database`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `sourceId` | 数据源 ID | 是 | `long` |
| `schemaName` | Schema / Database 名称 | 否 | `string` |
| `tableName` | 表名 | 是 | `string` |
| `limit` | 预览行数 | 否 | `int` |

**请求示例**

```http
GET /api/imports/database/preview?sourceId=1&schemaName=data_lake_platform&tableName=orders&limit=10
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.schemaName` | Schema 名称 | `string` |
| `data.tableName` | 表名 | `string` |
| `data.columns` | 字段预览列表 | `array` |
| `data.records` | 样例记录列表 | `array` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "schemaName": "data_lake_platform",
    "tableName": "orders",
    "columns": [
      {
        "fieldName": "order_id",
        "fieldType": "VARCHAR",
        "nullable": false,
        "sampleValue": "ORD20260405001"
      }
    ],
    "records": [
      {
        "order_id": "ORD20260405001",
        "amount": 129.9
      }
    ]
  },
  "timestamp": "2026-04-05T08:24:00Z",
  "requestId": "req-import-preview-001"
}
```

### 2.5.6 导入历史列表

- 接口地址：`GET /api/imports/history`
- 请求方式：`GET`
- 接口描述：按业务域、状态和时间范围查询导入历史。时间格式支持 `yyyy-MM-dd` 或 `yyyy-MM-ddTHH:mm:ss`。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `businessDomain` | 业务域筛选 | 否 | `string` |
| `status` | 状态筛选 | 否 | `string` |
| `startTime` | 开始时间 | 否 | `string` |
| `endTime` | 结束时间 | 否 | `string` |

**请求示例**

```http
GET /api/imports/history?businessDomain=TRADE&status=SUCCESS&startTime=2026-04-01&endTime=2026-04-05
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].importId` | 导入记录 ID | `long` |
| `data[].sourceId` | 数据源 ID | `long` |
| `data[].datasetName` | 数据集名称 | `string` |
| `data[].businessDomain` | 业务域 | `string` |
| `data[].formatType` | 格式类型 | `string` |
| `data[].status` | 导入状态 | `string` |
| `data[].recordCount` | 记录数 | `long` |
| `data[].errorMessage` | 错误信息 | `string` |
| `data[].createUser` | 操作人 ID | `long` |
| `data[].operatorName` | 操作人名称 | `string` |
| `data[].createTime` | 创建时间 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "importId": 1001,
      "sourceId": 2,
      "datasetName": "订单明细数据",
      "businessDomain": "TRADE",
      "formatType": "CSV",
      "status": "SUCCESS",
      "recordCount": 1280,
      "errorMessage": null,
      "createUser": 1,
      "operatorName": "admin",
      "createTime": "2026-04-05 08:20:00"
    }
  ],
  "timestamp": "2026-04-05T08:25:00Z",
  "requestId": "req-import-history-001"
}
```

### 2.5.7 导入详情

- 接口地址：`GET /api/imports/{importId}`
- 请求方式：`GET`
- 接口描述：查看单条导入记录详情和导入参数摘要。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `importId` | 导入记录 ID | 是 | `long` |

**请求示例**

```http
GET /api/imports/1001
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.importId` | 导入记录 ID | `long` |
| `data.sourceId` | 数据源 ID | `long` |
| `data.datasetName` | 数据集名称 | `string` |
| `data.businessDomain` | 业务域 | `string` |
| `data.formatType` | 格式类型 | `string` |
| `data.originalFileName` | 原始文件名 | `string` |
| `data.filePath` | 存储路径 | `string` |
| `data.status` | 状态 | `string` |
| `data.recordCount` | 记录数 | `long` |
| `data.errorMessage` | 错误信息 | `string` |
| `data.createUser` | 操作人 ID | `long` |
| `data.operatorName` | 操作人名称 | `string` |
| `data.sourceName` | 数据源名称 | `string` |
| `data.sourceType` | 数据源类型 | `string` |
| `data.createTime` | 创建时间 | `string` |
| `data.importParams` | 导入参数摘要 | `object` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "importId": 1001,
    "sourceId": 2,
    "datasetName": "订单明细数据",
    "businessDomain": "TRADE",
    "formatType": "CSV",
    "originalFileName": "orders.csv",
    "filePath": "fileimport://UTF-8/true/%2FUsers%2Fxyd%2FDesktop%2FDataLake%2Fstorage%2F20260405%2Forders.csv",
    "status": "SUCCESS",
    "recordCount": 1280,
    "errorMessage": null,
    "createUser": 1,
    "operatorName": "admin",
    "sourceName": "电商文件上传目录",
    "sourceType": "FILE",
    "createTime": "2026-04-05 08:20:00",
    "importParams": {
      "sourceId": 2,
      "storagePath": "/Users/xyd/Desktop/DataLake/storage/20260405/orders.csv"
    }
  },
  "timestamp": "2026-04-05T08:26:00Z",
  "requestId": "req-import-detail-001"
}
```

## 2.6 数据集管理

### 2.6.1 数据集列表

- 接口地址：`GET /api/datasets`
- 请求方式：`GET`
- 接口描述：按关键字、数据源、状态和业务域查询数据集列表。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `keyword` | 名称关键字 | 否 | `string` |
| `sourceId` | 数据源 ID | 否 | `long` |
| `status` | 数据集状态 | 否 | `string` |
| `businessDomain` | 业务域 | 否 | `string` |

**请求示例**

```http
GET /api/datasets?businessDomain=TRADE&status=READY
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].datasetId` | 数据集 ID | `long` |
| `data[].sourceId` | 数据源 ID | `long` |
| `data[].datasetName` | 数据集名称 | `string` |
| `data[].businessDomain` | 业务域 | `string` |
| `data[].formatType` | 格式类型 | `string` |
| `data[].recordCount` | 记录数 | `long` |
| `data[].fieldCount` | 字段数 | `int` |
| `data[].status` | 状态 | `string` |
| `data[].creator` | 创建人 ID | `long` |
| `data[].physicalTableName` | 物理表名 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "datasetId": 2001,
      "sourceId": 2,
      "datasetName": "订单明细数据",
      "businessDomain": "TRADE",
      "formatType": "CSV",
      "recordCount": 1280,
      "fieldCount": 12,
      "status": "READY",
      "creator": 1,
      "physicalTableName": "dl_dataset_2001"
    }
  ],
  "timestamp": "2026-04-05T08:27:00Z",
  "requestId": "req-dataset-list-001"
}
```

### 2.6.2 数据集详情

- 接口地址：`GET /api/datasets/{datasetId}`
- 请求方式：`GET`
- 接口描述：查询单个数据集详情。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |

**请求示例**

```http
GET /api/datasets/2001
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.datasetId` | 数据集 ID | `long` |
| `data.datasetName` | 数据集名称 | `string` |
| `data.businessDomain` | 业务域 | `string` |
| `data.sourceId` | 数据源 ID | `long` |
| `data.formatType` | 格式类型 | `string` |
| `data.recordCount` | 记录数 | `long` |
| `data.fieldCount` | 字段数 | `int` |
| `data.status` | 状态 | `string` |
| `data.creator` | 创建人 ID | `long` |
| `data.storagePath` | 存储路径 | `string` |
| `data.physicalTableName` | 物理表名 | `string` |
| `data.description` | 数据集说明 | `string` |
| `data.createTime` | 创建时间 | `string` |
| `data.updateTime` | 更新时间 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "datasetId": 2001,
    "datasetName": "订单明细数据",
    "businessDomain": "TRADE",
    "sourceId": 2,
    "formatType": "CSV",
    "recordCount": 1280,
    "fieldCount": 12,
    "status": "READY",
    "creator": 1,
    "storagePath": "/Users/xyd/Desktop/DataLake/storage/20260405/orders.csv",
    "physicalTableName": "dl_dataset_2001",
    "description": "文件导入生成的数据集",
    "createTime": "2026-04-05 08:20:00",
    "updateTime": "2026-04-05 08:20:00"
  },
  "timestamp": "2026-04-05T08:28:00Z",
  "requestId": "req-dataset-detail-001"
}
```

### 2.6.3 删除数据集

- 接口地址：`DELETE /api/datasets/{datasetId}`
- 请求方式：`DELETE`
- 接口描述：删除数据集。当前实现中若数据集被治理流程或治理日志引用，则禁止删除。
- 权限要求：`ACTION_dataset.delete`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |

**请求示例**

```http
DELETE /api/datasets/2001
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

`data` 为 `null`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": null,
  "timestamp": "2026-04-05T08:29:00Z",
  "requestId": "req-dataset-delete-001"
}
```

### 2.6.4 元数据列表

- 接口地址：`GET /api/metadata/{datasetId}`
- 请求方式：`GET`
- 接口描述：查询指定数据集的元数据字段列表。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |

**请求示例**

```http
GET /api/metadata/2001
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].fieldId` | 字段 ID | `long` |
| `data[].datasetId` | 数据集 ID | `long` |
| `data[].fieldName` | 展示字段名 | `string` |
| `data[].physicalColumnName` | 物理列名 | `string` |
| `data[].fieldType` | 字段类型 | `string` |
| `data[].nullable` | 是否可空 | `boolean` |
| `data[].sampleValue` | 样例值 | `string` |
| `data[].fieldOrder` | 字段顺序 | `int` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "fieldId": 1,
      "datasetId": 2001,
      "fieldName": "订单编号",
      "physicalColumnName": "order_id",
      "fieldType": "VARCHAR",
      "nullable": false,
      "sampleValue": "ORD20260405001",
      "fieldOrder": 1
    }
  ],
  "timestamp": "2026-04-05T08:30:00Z",
  "requestId": "req-metadata-001"
}
```

### 2.6.5 数据预览

- 接口地址：`GET /api/preview/{datasetId}`
- 请求方式：`GET`
- 接口描述：分页预览数据集记录，支持字段筛选和关键字匹配。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `pageNum` | 页码，默认 `1` | 否 | `int` |
| `pageSize` | 每页条数，默认 `10` | 否 | `int` |
| `field` | 筛选字段 | 否 | `string` |
| `keyword` | 关键字 | 否 | `string` |

**请求示例**

```http
GET /api/preview/2001?pageNum=1&pageSize=10&field=order_id&keyword=ORD2026
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

`data` 为分页对象，`records` 中每项为当前数据集的一条记录。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 1280,
    "records": [
      {
        "order_id": "ORD20260405001",
        "amount": 129.9,
        "status": "PAID"
      }
    ]
  },
  "timestamp": "2026-04-05T08:31:00Z",
  "requestId": "req-preview-001"
}
```

### 2.6.6 数据集导出

- 接口地址：`GET /api/datasets/{datasetId}/export`
- 请求方式：`GET`
- 接口描述：导出数据集记录，支持按字段和关键字筛选后导出。
- 权限要求：`ACTION_dataset.export`
- 请求数据类型：Query String
- 响应数据类型：文件流

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `format` | 导出格式，默认 `csv` | 否 | `string` |
| `field` | 筛选字段 | 否 | `string` |
| `keyword` | 关键字 | 否 | `string` |

**请求示例**

```http
GET /api/datasets/2001/export?format=xlsx&field=status&keyword=PAID
Authorization: Bearer <token>
```

**响应状态**

成功时返回 `200` 文件流；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

成功响应头示例：

| 参数名称 | 参数说明 |
| --- | --- |
| `Content-Type` | 文件内容类型 |
| `Content-Disposition` | 附件下载文件名 |

**响应示例**

```http
HTTP/1.1 200 OK
Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Content-Disposition: attachment; filename*=UTF-8''订单明细数据_20260405_083200.xlsx
```

## 2.7 查询分析

### 2.7.1 条件查询

- 接口地址：`POST /api/queries/filter`
- 请求方式：`POST`
- 接口描述：按条件查询数据集，支持旧口径单条件和新口径多条件组合。
- 权限要求：已登录用户
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `field` | 旧口径字段名 | 否 | `string` |
| `operator` | 旧口径操作符 | 否 | `string` |
| `value` | 旧口径值 | 否 | `string` |
| `valueTo` | 区间上界 | 否 | `string` |
| `conditions` | 新口径条件列表 | 否 | `array` |
| `logic` | 条件组合逻辑，支持 `AND / OR` | 否 | `string` |
| `sortField` | 排序字段 | 否 | `string` |
| `sortOrder` | 排序方向，默认 `ASC` | 否 | `string` |
| `pageNum` | 页码，默认 `1` | 否 | `int` |
| `pageSize` | 每页条数，默认 `20` | 否 | `int` |
| `exportScope` | 导出范围，支持 `PAGE / ALL` | 否 | `string` |

**请求示例**

```json
{
  "datasetId": 2001,
  "conditions": [
    { "field": "status", "operator": "EQ", "value": "PAID" },
    { "field": "amount", "operator": "GT", "value": "100" }
  ],
  "logic": "AND",
  "sortField": "amount",
  "sortOrder": "DESC",
  "pageNum": 1,
  "pageSize": 20
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

`data` 为分页对象，`records` 中每项为命中记录。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "pageNum": 1,
    "pageSize": 20,
    "total": 86,
    "records": [
      {
        "order_id": "ORD20260405001",
        "amount": 129.9,
        "status": "PAID"
      }
    ]
  },
  "timestamp": "2026-04-05T08:33:00Z",
  "requestId": "req-filter-001"
}
```

### 2.7.2 SQL 查询

- 接口地址：`POST /api/queries/sql`
- 请求方式：`POST`
- 接口描述：执行单表 SQL 查询。当前仅支持 `SELECT` 查询，且只能查询当前数据集对应的单表。
- 权限要求：已登录用户
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `sql` | SQL 语句 | 是 | `string` |

**请求示例**

```json
{
  "datasetId": 2001,
  "sql": "select order_id, amount from dl_dataset_2001 where amount > 100"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

`data` 为结果记录数组。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "order_id": "ORD20260405001",
      "amount": 129.9
    }
  ],
  "timestamp": "2026-04-05T08:34:00Z",
  "requestId": "req-sql-001"
}
```

### 2.7.3 SQL 分页查询

- 接口地址：`POST /api/queries/sql/page`
- 请求方式：`POST`
- 接口描述：执行单表 SQL 分页查询，支持分页和排序。
- 权限要求：已登录用户
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `sql` | SQL 语句 | 是 | `string` |
| `sortField` | 排序字段 | 否 | `string` |
| `sortOrder` | 排序方向 | 否 | `string` |
| `pageNum` | 页码 | 否 | `int` |
| `pageSize` | 每页条数 | 否 | `int` |

**请求示例**

```json
{
  "datasetId": 2001,
  "sql": "select order_id, amount, status from dl_dataset_2001",
  "sortField": "amount",
  "sortOrder": "DESC",
  "pageNum": 1,
  "pageSize": 20
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

`data` 为分页对象，`records` 中每项为查询结果记录。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "pageNum": 1,
    "pageSize": 20,
    "total": 1280,
    "records": [
      {
        "order_id": "ORD20260405001",
        "amount": 129.9,
        "status": "PAID"
      }
    ]
  },
  "timestamp": "2026-04-05T08:35:00Z",
  "requestId": "req-sql-page-001"
}
```

### 2.7.4 电商指标查询

- 接口地址：`POST /api/queries/ecommerce-metric`
- 请求方式：`POST`
- 接口描述：查询电商业务指标。当前支持 `ORDER_TREND / SALES_TREND / TOP_PRODUCTS / CATEGORY_SHARE / LOW_STOCK`。
- 权限要求：已登录用户
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `metricType` | 指标类型 | 是 | `string` |
| `timeField` | 时间字段 | 否 | `string` |
| `valueField` | 数值字段 | 否 | `string` |
| `categoryField` | 分类字段 | 否 | `string` |
| `productField` | 商品字段 | 否 | `string` |
| `quantityField` | 数量字段 | 否 | `string` |
| `stockThreshold` | 库存阈值 | 否 | `double` |
| `conditions` | 联动条件列表 | 否 | `array` |
| `logic` | 条件组合逻辑 | 否 | `string` |

**请求示例**

```json
{
  "datasetId": 2001,
  "metricType": "SALES_TREND",
  "timeField": "order_time",
  "valueField": "amount",
  "conditions": [
    { "field": "status", "operator": "EQ", "value": "PAID" }
  ],
  "logic": "AND"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.metricType` | 指标类型 | `string` |
| `data.chart` | 图表点位列表 | `array` |
| `data.table` | 表格结果列表 | `array` |
| `data.description` | 指标说明 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "metricType": "SALES_TREND",
    "chart": [
      { "name": "2026-04-01", "value": 9800.5 }
    ],
    "table": [
      { "date": "2026-04-01", "sales": 9800.5 }
    ],
    "description": "近一段时间销售额趋势"
  },
  "timestamp": "2026-04-05T08:36:00Z",
  "requestId": "req-metric-001"
}
```

### 2.7.5 数据分析摘要

- 接口地址：`GET /api/analysis/{datasetId}/summary`
- 请求方式：`GET`
- 接口描述：返回数据集摘要信息，包括记录数、空值数和重复值数。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |

**请求示例**

```http
GET /api/analysis/2001/summary
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.datasetId` | 数据集 ID | `long` |
| `data.recordCount` | 记录数 | `long` |
| `data.nullCount` | 空值数量 | `long` |
| `data.duplicateCount` | 重复值数量 | `long` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "datasetId": 2001,
    "recordCount": 1280,
    "nullCount": 12,
    "duplicateCount": 3
  },
  "timestamp": "2026-04-05T08:37:00Z",
  "requestId": "req-analysis-summary-001"
}
```

### 2.7.6 数据分布图

- 接口地址：`GET /api/analysis/{datasetId}/charts`
- 请求方式：`GET`
- 接口描述：返回默认维度下的数据分布图。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |

**请求示例**

```http
GET /api/analysis/2001/charts
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

`data` 为图表点位数组，每项包含 `name`、`value`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    { "name": "PAID", "value": 860 },
    { "name": "PENDING", "value": 420 }
  ],
  "timestamp": "2026-04-05T08:38:00Z",
  "requestId": "req-analysis-charts-001"
}
```

### 2.7.7 按条件聚合图表

- 接口地址：`POST /api/analysis/charts`
- 请求方式：`POST`
- 接口描述：根据维度字段和当前筛选条件聚合图表数据。
- 权限要求：已登录用户
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `dimensionField` | 维度字段 | 否 | `string` |
| `conditions` | 条件列表 | 否 | `array` |
| `logic` | 组合逻辑 | 否 | `string` |

**请求示例**

```json
{
  "datasetId": 2001,
  "dimensionField": "status",
  "conditions": [
    { "field": "amount", "operator": "GT", "value": "100" }
  ],
  "logic": "AND"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

`data` 为图表点位数组，每项包含 `name`、`value`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    { "name": "PAID", "value": 52 },
    { "name": "REFUNDED", "value": 8 }
  ],
  "timestamp": "2026-04-05T08:39:00Z",
  "requestId": "req-analysis-chart-filter-001"
}
```

### 2.7.8 数据集成预览

- 接口地址：`POST /api/queries/integration`
- 请求方式：`POST`
- 接口描述：对两个数据集执行 `JOIN / UNION` 并返回结果预览。
- 权限要求：已登录用户
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `leftDatasetId` | 左侧数据集 ID | 是 | `long` |
| `rightDatasetId` | 右侧数据集 ID | 是 | `long` |
| `mode` | 集成模式，支持 `JOIN / UNION` | 是 | `string` |
| `leftField` | 左关联字段 | 否 | `string` |
| `rightField` | 右关联字段 | 否 | `string` |
| `limit` | 预览条数 | 否 | `int` |

**请求示例**

```json
{
  "leftDatasetId": 2001,
  "rightDatasetId": 2003,
  "mode": "JOIN",
  "leftField": "product_id",
  "rightField": "product_id",
  "limit": 50
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.mode` | 集成模式 | `string` |
| `data.columns` | 字段列表 | `string[]` |
| `data.total` | 预览总数 | `int` |
| `data.records` | 预览记录列表 | `array` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "mode": "JOIN",
    "columns": ["order_id", "product_id", "product_name"],
    "total": 50,
    "records": [
      {
        "order_id": "ORD20260405001",
        "product_id": "P1001",
        "product_name": "无线鼠标"
      }
    ]
  },
  "timestamp": "2026-04-05T08:40:00Z",
  "requestId": "req-integration-001"
}
```

### 2.7.9 保存集成结果

- 接口地址：`POST /api/queries/integration/save`
- 请求方式：`POST`
- 接口描述：将集成结果保存为新数据集。
- 权限要求：已登录用户
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `leftDatasetId` | 左侧数据集 ID | 是 | `long` |
| `rightDatasetId` | 右侧数据集 ID | 是 | `long` |
| `mode` | 集成模式 | 是 | `string` |
| `leftField` | 左关联字段 | 否 | `string` |
| `rightField` | 右关联字段 | 否 | `string` |
| `limit` | 保存条数上限 | 否 | `int` |
| `outputDatasetName` | 输出数据集名称 | 是 | `string` |
| `outputBusinessDomain` | 输出业务域 | 否 | `string` |

**请求示例**

```json
{
  "leftDatasetId": 2001,
  "rightDatasetId": 2003,
  "mode": "JOIN",
  "leftField": "product_id",
  "rightField": "product_id",
  "limit": 100,
  "outputDatasetName": "订单商品集成结果",
  "outputBusinessDomain": "TRADE"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.datasetId` | 新数据集 ID | `long` |
| `data.datasetName` | 新数据集名称 | `string` |
| `data.businessDomain` | 业务域 | `string` |
| `data.recordCount` | 记录数 | `int` |
| `data.fieldCount` | 字段数 | `int` |
| `data.mode` | 集成模式 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "datasetId": 2010,
    "datasetName": "订单商品集成结果",
    "businessDomain": "TRADE",
    "recordCount": 100,
    "fieldCount": 18,
    "mode": "JOIN"
  },
  "timestamp": "2026-04-05T08:41:00Z",
  "requestId": "req-integration-save-001"
}
```

### 2.7.10 条件查询导出

- 接口地址：`POST /api/queries/filter/export`
- 请求方式：`POST`
- 接口描述：导出条件查询结果。
- 权限要求：`ACTION_query.export`
- 请求数据类型：`application/json`
- 响应数据类型：文件流

**请求参数**

请求体参数与“条件查询”一致，另需查询参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `format` | 导出格式，默认 `csv` | 否 | `string` |

**请求示例**

```http
POST /api/queries/filter/export?format=json
Authorization: Bearer <token>
Content-Type: application/json

{
  "datasetId": 2001,
  "conditions": [
    { "field": "status", "operator": "EQ", "value": "PAID" }
  ],
  "logic": "AND",
  "exportScope": "ALL"
}
```

**响应状态**

成功时返回 `200` 文件流；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

成功响应头包含 `Content-Type`、`Content-Disposition`。

**响应示例**

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Disposition: attachment; filename*=UTF-8''订单明细数据_filter_result_20260405_084200.json
```

### 2.7.11 SQL 查询导出

- 接口地址：`POST /api/queries/sql/export`
- 请求方式：`POST`
- 接口描述：导出 SQL 查询结果。
- 权限要求：`ACTION_query.export`
- 请求数据类型：`application/json`
- 响应数据类型：文件流

**请求参数**

请求体参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 数据集 ID | 是 | `long` |
| `sql` | SQL 语句 | 是 | `string` |

查询参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `format` | 导出格式，默认 `csv` | 否 | `string` |

**请求示例**

```http
POST /api/queries/sql/export?format=xlsx
Authorization: Bearer <token>
Content-Type: application/json

{
  "datasetId": 2001,
  "sql": "select order_id, amount from dl_dataset_2001 where amount > 100"
}
```

**响应状态**

成功时返回 `200` 文件流；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

成功响应头包含 `Content-Type`、`Content-Disposition`。

**响应示例**

```http
HTTP/1.1 200 OK
Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Content-Disposition: attachment; filename*=UTF-8''订单明细数据_sql_result_20260405_084300.xlsx
```

## 2.8 数据治理

### 2.8.1 算子列表

- 接口地址：`GET /api/governance/operators`
- 请求方式：`GET`
- 接口描述：查询治理算子列表，可按 `includeDisabled` 决定是否包含停用算子。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `includeDisabled` | 是否包含停用算子，默认 `false` | 否 | `boolean` |

**请求示例**

```http
GET /api/governance/operators?includeDisabled=true
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].operatorId` | 算子 ID | `long` |
| `data[].operatorName` | 算子名称 | `string` |
| `data[].operatorKey` | 算子键 | `string` |
| `data[].operatorType` | 算子类型 | `string` |
| `data[].description` | 算子说明 | `string` |
| `data[].status` | 算子状态 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "operatorId": 1,
      "operatorName": "空值填充",
      "operatorKey": "NULL_FILL",
      "operatorType": "CLEAN",
      "description": "将空值替换为指定内容",
      "status": "ENABLED"
    }
  ],
  "timestamp": "2026-04-05T08:44:00Z",
  "requestId": "req-governance-operators-001"
}
```

### 2.8.2 更新算子状态

- 接口地址：`POST /api/governance/operators/{operatorKey}/status`
- 请求方式：`POST`
- 接口描述：启用或停用治理算子。
- 权限要求：`ACTION_governance.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

路径参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `operatorKey` | 算子键 | 是 | `string` |

请求体参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `status` | 状态，支持 `ENABLED / DISABLED` | 是 | `string` |

**请求示例**

```json
{
  "status": "DISABLED"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“算子列表”中的算子对象字段一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "operatorId": 1,
    "operatorName": "空值填充",
    "operatorKey": "NULL_FILL",
    "operatorType": "CLEAN",
    "description": "将空值替换为指定内容",
    "status": "DISABLED"
  },
  "timestamp": "2026-04-05T08:45:00Z",
  "requestId": "req-governance-op-status-001"
}
```

### 2.8.3 流程列表

- 接口地址：`GET /api/governance/flows`
- 请求方式：`GET`
- 接口描述：查询治理流程列表。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/governance/flows
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].flowId` | 流程 ID | `long` |
| `data[].flowName` | 流程名称 | `string` |
| `data[].inputDatasetId` | 输入数据集 ID | `long` |
| `data[].inputDatasetName` | 输入数据集名称 | `string` |
| `data[].outputDatasetId` | 输出数据集 ID | `long` |
| `data[].outputDatasetName` | 输出数据集名称 | `string` |
| `data[].operatorChain` | 算子链 | `array` |
| `data[].creator` | 创建人 ID | `long` |
| `data[].creatorName` | 创建人名称 | `string` |
| `data[].createTime` | 创建时间 | `string` |
| `data[].updateTime` | 更新时间 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "flowId": 301,
      "flowName": "订单数据标准化流程",
      "inputDatasetId": 2001,
      "inputDatasetName": "订单明细数据",
      "outputDatasetId": 2011,
      "outputDatasetName": "订单治理结果",
      "operatorChain": [
        { "operatorKey": "NULL_FILL", "params": { "field": "status", "fillValue": "UNKNOWN" } }
      ],
      "creator": 1,
      "creatorName": "admin",
      "createTime": "2026-04-05 08:46:00",
      "updateTime": "2026-04-05 08:46:00"
    }
  ],
  "timestamp": "2026-04-05T08:46:00Z",
  "requestId": "req-governance-flow-list-001"
}
```

### 2.8.4 保存治理流程

- 接口地址：`POST /api/governance/flows`
- 请求方式：`POST`
- 接口描述：保存治理流程配置。
- 权限要求：`ACTION_governance.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `flowName` | 流程名称 | 是 | `string` |
| `datasetId` | 输入数据集 ID | 是 | `long` |
| `operatorChain` | 算子链 | 是 | `array` |

`operatorChain` 子项字段：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `operatorKey` | 算子键 | 是 | `string` |
| `params` | 算子参数 | 否 | `object` |

**请求示例**

```json
{
  "flowName": "订单数据标准化流程",
  "datasetId": 2001,
  "operatorChain": [
    {
      "operatorKey": "NULL_FILL",
      "params": {
        "field": "status",
        "fillValue": "UNKNOWN"
      }
    },
    {
      "operatorKey": "ORDER_DEDUP",
      "params": {
        "field": "order_id"
      }
    }
  ]
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.flowId` | 流程 ID | `long` |
| `data.flowName` | 流程名称 | `string` |
| `data.operatorCount` | 算子数量 | `int` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "flowId": 301,
    "flowName": "订单数据标准化流程",
    "operatorCount": 2
  },
  "timestamp": "2026-04-05T08:47:00Z",
  "requestId": "req-governance-flow-save-001"
}
```

### 2.8.5 执行治理流程

- 接口地址：`POST /api/governance/execute`
- 请求方式：`POST`
- 接口描述：按临时算子链执行治理，不要求先保存流程。成功时返回输出数据集和日志引用。
- 权限要求：`ACTION_governance.execute`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `datasetId` | 输入数据集 ID | 是 | `long` |
| `operatorChain` | 算子链 | 是 | `array` |
| `executionName` | 本次执行名称 | 否 | `string` |

`operatorChain` 子项字段与“保存治理流程”一致。

**请求示例**

```json
{
  "datasetId": 2001,
  "executionName": "订单清洗执行-1",
  "operatorChain": [
    {
      "operatorKey": "NULL_FILL",
      "params": {
        "field": "status",
        "fillValue": "UNKNOWN"
      }
    },
    {
      "operatorKey": "ORDER_DEDUP",
      "params": {
        "field": "order_id"
      }
    }
  ]
}
```

**响应状态**

成功 `200/0`；执行失败或参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.inputDatasetId` | 输入数据集 ID | `long` |
| `data.outputDatasetId` | 输出数据集 ID | `long` |
| `data.outputDatasetName` | 输出数据集名称 | `string` |
| `data.operatorCount` | 算子数量 | `int` |
| `data.inputRecordCount` | 输入记录数 | `long` |
| `data.outputRecordCount` | 输出记录数 | `long` |
| `data.abnormalHandledCount` | 异常处理条数 | `long` |
| `data.failedStep` | 失败步骤 | `string` |
| `data.failedReason` | 失败原因 | `string` |
| `data.logRef` | 对应日志 ID | `long` |
| `data.summary` | 执行摘要 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "inputDatasetId": 2001,
    "outputDatasetId": 2011,
    "outputDatasetName": "订单清洗执行-1",
    "operatorCount": 2,
    "inputRecordCount": 1280,
    "outputRecordCount": 1276,
    "abnormalHandledCount": 15,
    "failedStep": "",
    "failedReason": "",
    "logRef": 9001,
    "summary": "治理执行完成，共处理 2 个算子"
  },
  "timestamp": "2026-04-05T08:48:00Z",
  "requestId": "req-governance-exec-001"
}
```

## 2.9 任务调度

### 2.9.1 任务列表

- 接口地址：`GET /api/tasks`
- 请求方式：`GET`
- 接口描述：查询任务列表。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

无。

**请求示例**

```http
GET /api/tasks
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].taskId` | 任务 ID | `long` |
| `data[].taskName` | 任务名称 | `string` |
| `data[].taskType` | 任务类型 | `string` |
| `data[].targetId` | 目标对象 ID | `long` |
| `data[].targetName` | 目标对象名称 | `string` |
| `data[].cronExpr` | Cron 表达式 | `string` |
| `data[].status` | 状态 | `string` |
| `data[].retryPolicy` | 重试次数 | `int` |
| `data[].description` | 说明 | `string` |
| `data[].nextRunTime` | 下次执行时间 | `string` |
| `data[].lastRunTime` | 最近执行时间 | `string` |
| `data[].createUser` | 创建人 ID | `long` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "taskId": 11,
      "taskName": "每日订单导入任务",
      "taskType": "IMPORT",
      "targetId": 1001,
      "targetName": "订单明细数据",
      "cronExpr": "0 0 2 * * ?",
      "status": "ENABLED",
      "retryPolicy": 1,
      "description": "每日凌晨导入订单",
      "nextRunTime": "2026-04-06 02:00:00",
      "lastRunTime": "2026-04-05 02:00:00",
      "createUser": 1
    }
  ],
  "timestamp": "2026-04-05T08:49:00Z",
  "requestId": "req-task-list-001"
}
```

### 2.9.2 新建任务

- 接口地址：`POST /api/tasks`
- 请求方式：`POST`
- 接口描述：新建定时任务。当前仅支持 `IMPORT / GOVERNANCE` 两类任务。
- 权限要求：`ACTION_task.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `taskName` | 任务名称 | 是 | `string` |
| `taskType` | 任务类型 | 是 | `string` |
| `targetId` | 目标对象 ID | 是 | `long` |
| `cronExpr` | Cron 表达式 | 是 | `string` |
| `retryPolicy` | 重试次数 | 否 | `int` |
| `description` | 任务说明 | 否 | `string` |
| `status` | 初始状态 | 否 | `string` |

**请求示例**

```json
{
  "taskName": "每日订单导入任务",
  "taskType": "IMPORT",
  "targetId": 1001,
  "cronExpr": "0 0 2 * * ?",
  "retryPolicy": 1,
  "description": "每日凌晨导入订单",
  "status": "ENABLED"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“任务列表”中的任务对象字段一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "taskId": 11,
    "taskName": "每日订单导入任务",
    "taskType": "IMPORT",
    "targetId": 1001,
    "targetName": "订单明细数据",
    "cronExpr": "0 0 2 * * ?",
    "status": "ENABLED",
    "retryPolicy": 1,
    "description": "每日凌晨导入订单",
    "nextRunTime": "2026-04-06 02:00:00",
    "lastRunTime": null,
    "createUser": 1
  },
  "timestamp": "2026-04-05T08:50:00Z",
  "requestId": "req-task-create-001"
}
```

### 2.9.3 更新任务

- 接口地址：`PUT /api/tasks/{taskId}`
- 请求方式：`PUT`
- 接口描述：更新任务定义。
- 权限要求：`ACTION_task.manage`
- 请求数据类型：`application/json`
- 响应数据类型：`application/json`

**请求参数**

路径参数：

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `taskId` | 任务 ID | 是 | `long` |

请求体参数与“新建任务”一致。

**请求示例**

```json
{
  "taskName": "订单自动治理任务",
  "taskType": "GOVERNANCE",
  "targetId": 301,
  "cronExpr": "0 30 2 * * ?",
  "retryPolicy": 2,
  "description": "每日凌晨治理订单数据",
  "status": "PAUSED"
}
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“任务列表”中的任务对象字段一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "taskId": 12,
    "taskName": "订单自动治理任务",
    "taskType": "GOVERNANCE",
    "targetId": 301,
    "targetName": "订单数据标准化流程",
    "cronExpr": "0 30 2 * * ?",
    "status": "PAUSED",
    "retryPolicy": 2,
    "description": "每日凌晨治理订单数据",
    "nextRunTime": null,
    "lastRunTime": "2026-04-05 02:30:00",
    "createUser": 1
  },
  "timestamp": "2026-04-05T08:51:00Z",
  "requestId": "req-task-update-001"
}
```

### 2.9.4 立即执行任务

- 接口地址：`POST /api/tasks/{taskId}/trigger`
- 请求方式：`POST`
- 接口描述：立即执行任务。
- 权限要求：`ACTION_task.trigger`
- 请求数据类型：无请求体
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `taskId` | 任务 ID | 是 | `long` |

**请求示例**

```http
POST /api/tasks/11/trigger
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.taskId` | 任务 ID | `long` |
| `data.status` | 执行结果状态 | `string` |
| `data.logId` | 对应日志 ID | `long` |
| `data.message` | 执行结果说明 | `string` |
| `data.nextRunTime` | 下次执行时间 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "taskId": 11,
    "status": "SUCCESS",
    "logId": 9002,
    "message": "导入任务执行成功",
    "nextRunTime": "2026-04-06 02:00:00"
  },
  "timestamp": "2026-04-05T08:52:00Z",
  "requestId": "req-task-trigger-001"
}
```

### 2.9.5 暂停任务

- 接口地址：`POST /api/tasks/{taskId}/pause`
- 请求方式：`POST`
- 接口描述：暂停任务。
- 权限要求：`ACTION_task.manage`
- 请求数据类型：无请求体
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `taskId` | 任务 ID | 是 | `long` |

**请求示例**

```http
POST /api/tasks/11/pause
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“立即执行任务”的动作返回结构一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "taskId": 11,
    "status": "PAUSED",
    "logId": null,
    "message": "任务已暂停",
    "nextRunTime": null
  },
  "timestamp": "2026-04-05T08:53:00Z",
  "requestId": "req-task-pause-001"
}
```

### 2.9.6 恢复任务

- 接口地址：`POST /api/tasks/{taskId}/resume`
- 请求方式：`POST`
- 接口描述：恢复已暂停任务。
- 权限要求：`ACTION_task.manage`
- 请求数据类型：无请求体
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `taskId` | 任务 ID | 是 | `long` |

**请求示例**

```http
POST /api/tasks/11/resume
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“立即执行任务”的动作返回结构一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "taskId": 11,
    "status": "ENABLED",
    "logId": null,
    "message": "任务已恢复",
    "nextRunTime": "2026-04-06 02:00:00"
  },
  "timestamp": "2026-04-05T08:54:00Z",
  "requestId": "req-task-resume-001"
}
```

### 2.9.7 删除任务

- 接口地址：`DELETE /api/tasks/{taskId}`
- 请求方式：`DELETE`
- 接口描述：删除任务。当前实现中，运行中的任务和已有执行日志的任务禁止删除。
- 权限要求：`ACTION_task.manage`
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `taskId` | 任务 ID | 是 | `long` |

**请求示例**

```http
DELETE /api/tasks/11
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

`data` 为 `null`。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": null,
  "timestamp": "2026-04-05T08:55:00Z",
  "requestId": "req-task-delete-001"
}
```

## 2.10 日志监控

### 2.10.1 日志列表

- 接口地址：`GET /api/task-logs`
- 请求方式：`GET`
- 接口描述：按任务类型、状态、操作人、时间范围和关键字查询任务日志。时间格式支持 `yyyy-MM-dd` 或 `yyyy-MM-dd HH:mm:ss`。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `taskType` | 任务类型 | 否 | `string` |
| `status` | 执行状态 | 否 | `string` |
| `operatorUser` | 操作人 ID | 否 | `long` |
| `startTime` | 开始时间 | 否 | `string` |
| `endTime` | 结束时间 | 否 | `string` |
| `keyword` | 关键字 | 否 | `string` |

**请求示例**

```http
GET /api/task-logs?taskType=GOVERNANCE&status=FAILED&startTime=2026-04-01&endTime=2026-04-05
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

数组元素字段如下：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data[].logId` | 日志 ID | `long` |
| `data[].taskId` | 任务 ID | `long` |
| `data[].taskName` | 任务名称 | `string` |
| `data[].taskType` | 任务类型 | `string` |
| `data[].targetId` | 目标对象 ID | `long` |
| `data[].status` | 执行状态 | `string` |
| `data[].startTime` | 开始时间 | `string` |
| `data[].endTime` | 结束时间 | `string` |
| `data[].duration` | 耗时（秒） | `long` |
| `data[].executionSummary` | 执行摘要 | `string` |
| `data[].errorMessage` | 错误信息 | `string` |
| `data[].operatorUser` | 操作人 ID | `long` |
| `data[].operatorName` | 操作人名称 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": [
    {
      "logId": 9003,
      "taskId": 12,
      "taskName": "订单自动治理任务",
      "taskType": "GOVERNANCE",
      "targetId": 301,
      "status": "FAILED",
      "startTime": "2026-04-05 02:30:00",
      "endTime": "2026-04-05 02:30:03",
      "duration": 3,
      "executionSummary": "治理执行失败",
      "errorMessage": "算子 STATUS_NORMALIZE 缺少 field 参数",
      "operatorUser": 1,
      "operatorName": "admin"
    }
  ],
  "timestamp": "2026-04-05T08:56:00Z",
  "requestId": "req-log-list-001"
}
```

### 2.10.2 日志详情

- 接口地址：`GET /api/task-logs/{logId}`
- 请求方式：`GET`
- 接口描述：查看日志详情，包括输入参数、执行步骤和结构化失败原因。
- 权限要求：已登录用户
- 请求数据类型：Query String
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `logId` | 日志 ID | 是 | `long` |

**请求示例**

```http
GET /api/task-logs/9003
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`。

**响应参数**

在日志列表字段基础上，`data` 还包含：

| 参数名称 | 参数说明 | 类型 |
| --- | --- | --- |
| `data.inputParams` | 输入参数对象 | `object` |
| `data.executionSteps` | 执行步骤列表 | `array` |
| `data.failureReason` | 结构化失败原因 | `object` |
| `data.createTime` | 创建时间 | `string` |

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "logId": 9003,
    "taskId": 12,
    "taskName": "订单自动治理任务",
    "taskType": "GOVERNANCE",
    "targetId": 301,
    "status": "FAILED",
    "startTime": "2026-04-05 02:30:00",
    "endTime": "2026-04-05 02:30:03",
    "duration": 3,
    "executionSummary": "治理执行失败",
    "errorMessage": "算子 STATUS_NORMALIZE 缺少 field 参数",
    "inputParams": {
      "datasetId": 2001,
      "executionName": "订单清洗执行-1"
    },
    "executionSteps": [
      {
        "stepIndex": 1,
        "stepName": "NULL_FILL",
        "status": "SUCCESS",
        "detail": "算子执行完成"
      },
      {
        "stepIndex": 2,
        "stepName": "STATUS_NORMALIZE",
        "status": "FAILED",
        "detail": "缺少 field 参数"
      }
    ],
    "failureReason": {
      "code": "GOVERNANCE_OPERATOR_FAILED",
      "step": "step-2:STATUS_NORMALIZE",
      "reason": "缺少 field 参数",
      "rawMessage": "算子 STATUS_NORMALIZE 缺少 field 参数"
    },
    "operatorUser": 1,
    "operatorName": "admin",
    "createTime": "2026-04-05 02:30:03"
  },
  "timestamp": "2026-04-05T08:57:00Z",
  "requestId": "req-log-detail-001"
}
```

### 2.10.3 失败回放

- 接口地址：`POST /api/task-logs/{logId}/replay`
- 请求方式：`POST`
- 接口描述：根据指定日志重新触发任务回放。当前仅支持由调度任务生成的日志记录一键回放。
- 权限要求：`ACTION_log.replay`
- 请求数据类型：无请求体
- 响应数据类型：`application/json`

**请求参数**

| 参数名称 | 参数说明 | 必填 | 类型 |
| --- | --- | --- | --- |
| `logId` | 日志 ID | 是 | `long` |

**请求示例**

```http
POST /api/task-logs/9003/replay
Authorization: Bearer <token>
```

**响应状态**

成功 `200/0`；参数错误 `400/40001`；未登录 `401`；无权限 `403`。

**响应参数**

与“立即执行任务”的动作返回结构一致。

**响应示例**

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "taskId": 12,
    "status": "SUCCESS",
    "logId": 9004,
    "message": "任务回放执行成功",
    "nextRunTime": "2026-04-06 02:30:00"
  },
  "timestamp": "2026-04-05T08:58:00Z",
  "requestId": "req-log-replay-001"
}
```

## 2.11 导出接口汇总

当前已实现的导出接口如下：

| 接口地址 | 请求方式 | 导出对象 | 权限要求 | 支持格式 | 返回类型 |
| --- | --- | --- | --- | --- | --- |
| `GET /api/datasets/{datasetId}/export` | `GET` | 数据集记录 | `ACTION_dataset.export` | `csv / json / xlsx` | 文件流 |
| `POST /api/queries/filter/export` | `POST` | 条件查询结果 | `ACTION_query.export` | `csv / json / xlsx` | 文件流 |
| `POST /api/queries/sql/export` | `POST` | SQL 查询结果 | `ACTION_query.export` | `csv / json / xlsx` | 文件流 |

补充说明：

1. 当前没有独立的日志导出后端接口，因此本文档不把日志导出写成已实现 API。
2. 导出文件名会自动追加时间戳。
3. 返回头中会包含 `Content-Type` 与 `Content-Disposition`。

# 结语

以上接口设计文档以当前项目冻结代码为依据，对电商数据湖管理平台已实现的后端接口进行了分类说明。文档重点保证接口地址、请求参数、权限要求、返回结构和当前边界与代码一致，可直接作为课程项目报告、前后端联调和教师审阅材料使用。
