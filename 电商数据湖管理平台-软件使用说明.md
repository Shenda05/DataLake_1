**电商数据湖管理平台**

**软件使用说明**

Software User Manual

项目名称：电商数据湖管理平台（Web 端）

摘要：本文档依据当前仓库冻结版本的实际实现，对电商数据湖管理平台的登录方式、主要功能模块、实际操作步骤和使用注意事项进行说明。文档面向课程项目报告、教师审阅和课堂演示场景，重点覆盖数据源管理、数据接入、数据集管理、查询分析、数据治理、任务调度、日志监控和用户权限等模块，内容全部以当前系统已实现能力为准，不扩写未实现功能。

相关文档：

1. 《电商数据湖管理平台-产品功能设计文档-V3》
2. 《电商数据湖管理平台-模块设计文档》
3. 《电商数据湖管理平台-接口设计文档》
4. 《电商数据湖管理平台-数据库设计文档》
5. 《电商数据湖管理平台-用户故事文档-V2》
6. `docs/user-manual.md`
7. `docs/demo-runbook.md`
8. `docs/freeze-release-note.md`
9. `README.md`

版权所有：除特别允许否则只在项目组内部教学、课程开发和课程报告使用。

**修改记录：**

| 日期 | 版本 | 说明 | 作者 |
| --- | --- | --- | --- |
| 2026/04/06 | V1.0 | 依据教师模板并结合当前冻结代码实现新增软件使用说明文档 | 肖溢丹 |

# 目录

- 1 文档简介
  - 1.1 目的
  - 1.2 范围
  - 1.3 定义、首字母缩写词和缩略语
  - 1.4 参考资料
- 2 软件简介
  - 2.1 登录功能
    - 2.1.1 管理员登录
    - 2.1.2 普通用户登录
  - 2.2 首页与公共导航
  - 2.3 数据源管理
  - 2.4 数据接入
    - 2.4.1 文件导入
    - 2.4.2 数据库表导入
    - 2.4.3 导入历史与详情
  - 2.5 数据集管理
  - 2.6 查询分析
    - 2.6.1 条件查询
    - 2.6.2 SQL 查询
    - 2.6.3 电商指标分析
    - 2.6.4 数据集成
  - 2.7 数据治理
  - 2.8 任务调度
  - 2.9 日志监控
  - 2.10 用户与权限
  - 2.11 注意事项

# 1 文档简介

## 1.1 目的

撰写本文档的目的在于说明电商数据湖管理平台当前版本的主要功能和实际操作方式，便于使用者了解如何登录系统、使用主要模块并完成课程演示中的关键操作，同时也为系统维护和课程答辩提供统一的使用依据。

## 1.2 范围

本文档面向当前冻结版本的电商数据湖管理平台，覆盖以下内容：

1. 系统登录方式和角色入口。
2. 首页与公共导航的使用方法。
3. 数据源管理、数据接入、数据集管理、查询分析、数据治理、任务调度、日志监控、用户与权限等主要模块的使用说明。
4. 当前系统的实际边界和使用注意事项。

## 1.3 定义、首字母缩写词和缩略语

| 名称 | 说明 |
| --- | --- |
| CSV | 逗号分隔文本文件格式，当前系统支持导入与导出 |
| JSON | JavaScript Object Notation，当前系统支持文件导入与结果导出 |
| Excel / XLSX | 电子表格文件格式，当前系统支持文件导入与结果导出 |
| SQL | Structured Query Language，当前系统支持当前数据集单表查询 |
| JWT | JSON Web Token，系统当前用于登录鉴权 |
| IMPORT | 导入类任务类型 |
| GOVERNANCE | 治理类任务类型 |
| JOIN | 数据集成中的关联方式 |
| UNION | 数据集成中的合并方式 |

## 1.4 参考资料

1. 《电商数据湖管理平台-产品功能设计文档-V3》
2. 《电商数据湖管理平台-模块设计文档》
3. 《电商数据湖管理平台-接口设计文档》
4. 《电商数据湖管理平台-数据库设计文档》
5. 《电商数据湖管理平台-用户故事文档-V2》
6. `docs/user-manual.md`
7. `docs/demo-runbook.md`
8. `docs/freeze-release-note.md`
9. `README.md`

# 2 软件简介

电商数据湖管理平台是一个面向课程项目的 Web 端管理系统，用于完成电商数据的接入、数据集管理、查询分析、数据治理、任务调度与日志监控。系统在保留通用数据湖平台骨架的基础上，围绕订单、商品、库存等电商业务域完成了电商化改造，当前主流程已覆盖“登录与权限 → 数据接入 → 数据集管理 → 查询分析 → 数据治理 → 任务调度 → 日志监控”。

## 2.1 登录功能

系统当前登录用户分为管理员和普通用户。登录成功后，系统会根据当前角色返回对应的菜单权限和操作权限，页面左侧导航与按钮会按权限进行收口。

### 2.1.1 管理员登录

管理员可使用以下默认账号进入系统：

- 用户名：`admin`
- 密码：`admin123`

管理员可使用的主要能力包括：

1. 查看首页完整信息与快捷入口。
2. 新增、编辑、启停、测试和删除数据源。
3. 执行文件导入和数据库表导入。
4. 删除未被引用的数据集。
5. 保存治理流程、执行治理。
6. 创建、编辑、暂停、恢复和立即执行任务。
7. 查看日志详情、导出日志、回放失败日志。
8. 管理用户和角色权限。

实际操作步骤如下：

1. 打开登录页 `/login`。
2. 输入管理员用户名和密码。
3. 点击“登录”按钮。
4. 登录成功后，页面跳转到首页或首个有权限菜单。

页面可见结果如下：

1. 顶部栏显示当前用户信息。
2. 左侧菜单显示完整管理功能。
3. 首页展示完整统计卡片、快捷入口和最近任务/日志信息。

### 2.1.2 普通用户登录

普通用户可使用以下默认账号进入系统：

- 用户名：`operator`
- 密码：`operator123`

普通用户可使用的主要能力包括：

1. 查看首页与授权菜单。
2. 使用数据接入、数据集管理、查询分析、数据治理、任务调度和日志监控等已授权功能。
3. 导出数据集、查询结果和日志结果（前提是拥有对应操作权限）。

实际操作步骤如下：

1. 打开登录页 `/login`。
2. 输入普通用户名和密码。
3. 点击“登录”按钮。

页面可见结果如下：

1. 登录成功后，系统进入首页或首个有权限页面。
2. 普通用户只会看到当前角色拥有的菜单。
3. 若管理员修改了该角色的菜单权限或操作权限，当前会话会在下一次 profile 同步后自动更新。

## 2.2 首页与公共导航

首页是系统的总览页面，所有已登录用户都可按权限访问。首页当前主要用于展示系统总体情况和模块级快捷跳转。

首页当前展示内容包括：

1. 数据集总数。
2. 数据源总数。
3. 调度任务数。
4. 最近失败任务数。
5. 近 7 天订单量趋势。
6. 近 7 天销售额趋势。
7. 热销商品 Top5。
8. 低库存商品数。
9. 最近任务。
10. 最近执行日志。

进入方式：

1. 登录后系统默认进入首页，或
2. 在左侧菜单点击“电商仪表盘”。

实际操作说明：

1. 可通过首页快捷按钮进入数据源管理、数据接入、查询分析、任务与日志等模块。
2. 点击统计卡片或核心图表时，可跳转到对应模块页。
3. 普通用户只看到当前角色有权限访问的快捷入口。

页面可见结果如下：

1. 首页内容会按权限动态收口。
2. 最近任务和最近日志会显示最新执行情况。
3. 统计卡片和图表只支持模块级跳转，不携带图表点位级筛选条件。

## 2.3 数据源管理

数据源管理模块用于配置系统可用的数据源。当前仅支持两类数据源：

1. `FILE`：文件型数据源。
2. `MYSQL`：数据库型数据源。

使用权限：

1. 仅管理员可新增、编辑、启停、测试和删除数据源。

进入方式：

1. 左侧菜单点击“数据源管理”。

主要功能说明：

1. 新增数据源。
2. 编辑已有数据源。
3. 启用或停用数据源。
4. 测试连接。
5. 删除未被引用的数据源。

实际操作步骤如下：

1. 进入“数据源管理”页。
2. 点击“新增数据源”或在列表中点击“编辑”。
3. 选择数据源类型：
   - `FILE`
   - `MYSQL`
4. 输入数据源名称、说明等信息。
5. 若选择 `MYSQL`，需补充连接信息，并设置同连接处理策略：
   - `ALLOW`
   - `WARN`
   - `REJECT`
6. 点击“保存”。
7. 可在列表中继续执行“测试连接”“启停切换”或“删除”。

页面可见结果如下：

1. 同名数据源会被直接拒绝保存。
2. 若 `MYSQL` 连接重复且策略为 `WARN`，系统允许保存并返回提示信息。
3. 若策略为 `REJECT`，系统直接阻止保存。
4. `FILE` 数据源测试连接会返回“文件数据源无需远程连接，状态正常”。
5. 若数据源已被数据集引用，则不能删除。

## 2.4 数据接入

数据接入模块用于把外部数据导入为平台内部可管理的数据集。当前包含文件导入、数据库表导入和导入历史查看三部分。

进入方式：

1. 左侧菜单点击“电商数据接入”。

使用权限：

1. 管理员可执行文件导入和数据库表导入。
2. 数据库表导入需要具备对应权限，通常由管理员执行。

### 2.4.1 文件导入

当前系统支持以下文件格式：

1. `CSV`
2. `JSON`
3. `Excel`

实际操作步骤如下：

1. 进入“电商数据接入”页，默认停留在文件导入区域。
2. 选择一个可用的数据源。
3. 输入数据集名称。
4. 选择业务域。
5. 上传文件。
6. 根据文件格式配置解析选项：
   - `CSV`：设置编码方式、是否首行为表头
   - `Excel`：设置是否首行为表头
   - `JSON`：按对象数组自动解析，编码方式和表头开关自动忽略
7. 点击“导入”。

页面可见结果如下：

1. 导入成功后，系统生成新数据集。
2. 导入历史新增一条成功记录。
3. 记录中可查看格式、业务域、状态、记录数、操作人和创建时间。
4. 若文件格式非法或解析失败，页面会显示明确失败原因。

### 2.4.2 数据库表导入

当前数据库表导入仅支持 `MYSQL` 数据源，且采用“选择 `Schema / Database` + 选择表”的方式导入。

实际操作步骤如下：

1. 进入“电商数据接入”页。
2. 切换到数据库表导入区域。
3. 选择一个可用的 `MYSQL` 数据源。
4. 等待系统自动加载 `Schema / Database` 下拉。
5. 选择目标 `Schema / Database`。
6. 选择数据库表。
7. 点击“预览数据”查看字段和样例行。
8. 输入数据集名称、业务域和描述。
9. 点击“导入为数据集”。

页面可见结果如下：

1. `Schema / Database` 下拉由系统自动识别。
2. 切换数据库后，表列表会同步刷新。
3. 预览区会显示字段名、字段类型和样例记录。
4. 导入成功后，系统生成新的数据集和导入历史记录。

### 2.4.3 导入历史与详情

导入历史用于追踪导入结果，是查看导入成功、失败和后续任务重跑的重要依据。

实际操作步骤如下：

1. 在“电商数据接入”页下方查看导入历史区域。
2. 按业务域、状态和时间范围筛选导入记录。
3. 点击某条记录查看详情。

页面可见结果如下：

1. 每条记录会显示数据集名称、业务域、导入格式、状态、记录数和创建时间。
2. 详情中可查看导入参数摘要、文件路径或数据库表快照标识。
3. 失败记录会显示明确错误原因。

## 2.5 数据集管理

数据集管理模块用于查看平台中的数据资产，并对数据集执行详情查看、元数据查看、预览、导出和删除等操作。

进入方式：

1. 左侧菜单点击“电商数据集管理”。

使用权限：

1. 所有已授权用户可查看数据集列表、元数据和预览。
2. 管理员可删除未被引用的数据集。

主要功能说明：

1. 查看数据集列表。
2. 按业务域、来源、状态和关键字筛选数据集。
3. 查看数据集详情。
4. 查看字段元数据。
5. 分页预览数据。
6. 导出 `CSV / JSON / Excel`。
7. 删除未被引用的数据集。

实际操作步骤如下：

1. 进入“电商数据集管理”页。
2. 通过筛选条件缩小列表范围。
3. 选择一个数据集。
4. 查看右侧详情区、元数据区和预览区。
5. 在预览区按字段和关键字继续过滤。
6. 如需导出，点击导出按钮选择对应格式。
7. 如为管理员且确认数据集未被引用，可执行删除。

页面可见结果如下：

1. 列表中显示数据集名称、业务域、来源数据源、记录数、字段数、状态等信息。
2. 元数据区显示字段名、物理列名、字段类型、可空、样例值和字段顺序。
3. 预览区支持分页显示。
4. 被治理流程、日志或导入记录引用时，系统会阻止删除。

## 2.6 查询分析

查询分析模块用于完成数据检索、SQL 查询、电商指标分析和数据集成。当前该模块是查询与分析的统一入口。

进入方式：

1. 左侧菜单点击“电商查询分析”。

使用权限：

1. 所有已授权用户可根据权限执行查询、查看图表和导出结果。

### 2.6.1 条件查询

条件查询用于按字段组合条件检索当前数据集。

实际操作步骤如下：

1. 进入“电商查询分析”页。
2. 选择一个数据集。
3. 添加一个或多个查询条件。
4. 设置 `AND / OR` 逻辑。
5. 根据需要设置排序字段、排序顺序和分页参数。
6. 若涉及时间字段，可使用时间范围查询。
7. 点击“查询”。

页面可见结果如下：

1. 返回分页结果表格。
2. 更新结果总数和当前页记录。
3. 页面可联动展示基础分析结果或分布信息。
4. 查询结果支持导出。

### 2.6.2 SQL 查询

SQL 查询用于对当前数据集执行单表查询。

实际操作步骤如下：

1. 在查询分析页切换到 SQL 查询区域。
2. 选择数据集。
3. 输入 `SELECT` 语句，例如当前数据集单表查询语句。
4. 点击“执行 SQL”。
5. 根据需要查看分页和排序结果。
6. 点击导出按钮可导出查询结果。

页面可见结果如下：

1. 系统只允许当前数据集单表 `SELECT` 查询。
2. 查询成功后返回字段列和结果集。
3. 非法 SQL、注释语句、带分号语句或非单表查询会被拒绝。

### 2.6.3 电商指标分析

电商指标分析用于展示当前系统已经实现的核心业务图表。

当前真实指标包括：

1. 订单量趋势
2. 销售额趋势
3. 热销商品排行
4. 分类销售占比
5. 库存预警

实际操作步骤如下：

1. 进入查询分析页中的电商指标区域。
2. 选择数据集或指标类型。
3. 根据需要调整时间范围或筛选条件。

页面可见结果如下：

1. 图表会按当前筛选条件更新。
2. 支持展示趋势类和排行类电商指标。
3. 图表结果与当前数据集上下文保持一致。

### 2.6.4 数据集成

数据集成是查询分析页中的子标签能力，不是独立菜单模块。当前仅支持基础 `JOIN / UNION` 两种模式。

实际操作步骤如下：

1. 进入查询分析页中的数据集成区域。
2. 选择左侧数据集和右侧数据集。
3. 选择集成模式：
   - `JOIN`
   - `UNION`
4. 若使用 `JOIN`，继续选择左右关联字段。
5. 点击“预览结果”。
6. 如结果符合预期，输入新数据集名称并保存。

页面可见结果如下：

1. 系统返回集成预览结果。
2. 保存成功后，会生成新的数据集。
3. 若字段名冲突，系统会自动处理字段名。

## 2.7 数据治理

数据治理模块用于对已有数据集执行基础清洗、去重和标准化处理，并将结果保存为新的输出数据集。

进入方式：

1. 左侧菜单点击“电商数据治理”。

使用权限：

1. 具备治理权限的用户可查看算子、保存流程和执行治理。

当前系统内置的治理算子包括：

1. `NULL_FILL`
2. `DEDUPLICATE`
3. `FIELD_CONVERT`
4. `FILTER_KEEP`
5. `ORDER_DEDUP`
6. `AMOUNT_NORMALIZE`
7. `TIME_NORMALIZE`
8. `CATEGORY_NORMALIZE`
9. `STATUS_NORMALIZE`

实际操作步骤如下：

1. 进入“电商数据治理”页。
2. 选择输入数据集。
3. 添加治理步骤。
4. 根据需要编辑 JSON 参数。
5. 可通过上移、下移调整步骤顺序。
6. 如需复用流程，先点击“保存流程”。
7. 点击“执行治理”。

页面可见结果如下：

1. 成功时会生成新的输出数据集。
2. 页面显示输入记录数、输出记录数和异常处理条数。
3. 若执行失败，页面会显示失败步骤和失败原因。
4. 停用中的算子不能用于保存或执行。

## 2.8 任务调度

任务调度模块用于管理定时执行任务。当前系统只支持两类任务：

1. `IMPORT`
2. `GOVERNANCE`

进入方式：

1. 左侧菜单点击“电商任务调度”。

使用权限：

1. 具备任务管理权限的用户可创建、编辑、暂停、恢复和立即执行任务。

实际操作步骤如下：

1. 进入“电商任务调度”页。
2. 点击“新建任务”。
3. 选择任务类型：
   - `IMPORT`
   - `GOVERNANCE`
4. 选择目标对象：
   - `IMPORT` 任务选择导入历史记录
   - `GOVERNANCE` 任务选择治理流程
5. 输入任务名称、Cron 表达式、重试策略和说明。
6. 点击“保存”。
7. 可在列表中继续执行“立即执行”“暂停”“恢复”等操作。

页面可见结果如下：

1. 任务列表显示任务名称、类型、状态和最近执行信息。
2. 任务支持自动刷新。
3. 任务页支持筛选条件保存与常用视图管理。
4. 当前调度通过应用内轮询执行，不依赖 Quartz。

## 2.9 日志监控

日志监控模块用于查看导入任务、治理任务以及手动执行产生的日志记录，并支持失败日志回放。

进入方式：

1. 左侧菜单点击“任务与日志监控”。

使用权限：

1. 具备日志查看权限的用户可查看日志列表和详情。
2. 具备 `log.replay` 权限的用户可回放失败日志。
3. 具备 `log.export` 权限的用户可导出当前筛选结果。

实际操作步骤如下：

1. 进入“任务与日志监控”页。
2. 通过任务类型、状态、操作人、时间范围和关键字筛选日志。
3. 点击某条日志查看详情。
4. 在详情中查看输入参数、执行步骤、错误信息和结构化失败原因。
5. 对任务生成的失败日志，可点击“回放”。
6. 如需导出日志结果，可在当前筛选结果基础上导出。

页面可见结果如下：

1. 日志列表展示任务名称、任务类型、开始时间、结束时间、耗时、状态和执行摘要。
2. 日志详情会显示 `inputParams`、`executionSteps` 和 `failureReason`。
3. 失败日志回放成功后，日志列表条数会增加。
4. 当前日志导出为前端基于筛选结果的本地导出，不依赖独立后端导出接口。

## 2.10 用户与权限

用户与权限模块用于管理系统用户、角色说明、菜单权限和操作权限。当前仅管理员可见。

进入方式：

1. 左侧菜单点击“用户与权限”。

使用权限：

1. 仅管理员可进入和操作。

主要功能说明：

1. 新增用户。
2. 编辑用户基本信息、角色和状态。
3. 停用或删除用户。
4. 修改角色说明。
5. 配置菜单权限。
6. 配置操作权限。

实际操作步骤如下：

1. 进入“用户与权限”页。
2. 在用户列表中新增或选择已有用户。
3. 编辑用户名、角色和状态后保存。
4. 选择某个角色，修改其菜单权限和操作权限。
5. 点击“保存角色配置”。

页面可见结果如下：

1. 用户列表会显示用户名、角色和状态。
2. 角色权限区会按菜单权限和分组操作权限展示。
3. 权限修改后，相关用户的当前会话会在同步后自动感知变化。
4. 当前系统会阻止管理员误删自己或破坏管理员关键权限边界。

## 2.11 注意事项

1. SQL 查询当前仅支持当前数据集单表 `SELECT`，不支持多表 `JOIN`。
2. 数据库表导入当前不支持自定义 SQL，只支持选择 `Schema / Database + 表` 导入。
3. 数据集成能力当前通过查询分析页子标签提供，不是独立一级菜单。
4. 当前任务调度为应用内轮询，不是 Quartz。
5. 日志页导出为前端基于当前筛选结果本地生成，不是独立后端导出接口。
6. 首页统计卡片和核心图表支持模块级跳转，但不携带图表点位级筛选条件。
7. 导入任务重跑时，原始文件或原始数据库表需要仍然可访问。
8. 被治理流程、日志或导入记录引用的数据集不可删除。
